# Design system audit

Four parallel passes over colour, tokens and control vocabulary (Document 2),
and the seven-dimension audit of 2026-09-02 with what it left open (Document
3, at the end). A worklist, not a record of work done.

---

# DOCUMENT 2 — Design system audit: colour and control vocabulary

HEAD `3fc77b1`. Commit `8071823` ("green means 'this is on', not 'click this'") already landed and is included in everything below.

## 1. Current state — colour

**The primary-button green is already gone.** `.btn-primary` is a raised neutral across all four states — `bg-4` fill, `bg-5` hover, `line-2` border (`buttons.css:65-85`) — with the rationale at `buttons.css:54-64`. The green CTA glow tokens were deleted (`tokens.css` diff in `8071823`). 24 call sites inherit it. Nothing re-greens it.

**Green still paints 115 rules / 184 declarations across 21 of 23 stylesheets** (pass 3's enumeration; I spot-checked the reference counts and they hold: in `src/styles/*.css` alone, `var(--accent)` 101, `var(--accent-rgb)` 48, `--color-accent-green` 20, `--ella-green` 14, `--success` 11). Pass 3's three groups:

- **(a) live/active state — 67 rules.** Nav rail active view, running progress fills, playing cue, session badges, toggles that are on.
- **(b) a control that is merely available — 25 rules.** Fetch hover flare (`buttons.css:190-208`), Generate hover glow (`buttons.css:298`, `:316`), four resize-handle rails (`resize.css:34/44/58/68`), five native `accent-color` tints, two AI citation styles.
- **(c) decoration — 23 rules.** Of which **15 are dead**: `var(--marker-color, var(--accent))` / `--pr-color` / `--ghost-color` fallbacks whose custom property is always set inline from JSX (`Timeline.tsx:148/210/528/546`, `ReviewPanel.tsx:1505`, `PeoplePanel.tsx:243`), all resolving through `AVATAR_COLORS` (`review.ts:789`), which never returns undefined. Those 15 green tokens never paint a pixel.

**Inside group (a), 18 rules are success/outcome, not liveness** — done, ok, pass, approved, authorized, copied. That is a separate semantic wearing the same colour.

**One hex answers to seven names.** `#6CFF8D` is `--ella-green` (`tokens.css:10`), `--success` (`tokens.css:64`), `--color-accent-green` (`tokens.css:123`); `--stroke-green` (`tokens.css:53`) and `--ring-green` (`tokens.css:102-104`) derive from its rgba; `--accent` aliases it at `base.css:58` with `--accent-rgb` at `:59`. **149 declarations flow through that one alias line.**

**The stated rule contradicts itself in the tree.** `buttons.css:186-189` says, in as many words, "colour contract: green marks the primary action and live states, nothing else" — and commit `8071823` says green means "this is on", not "click this". Both are in the repo. Neither is enforced by a test.

**Green is also an identity colour, in a place CSS cannot see.** `AVATAR_COLORS` (`review.ts:789`) contains `#2dd4bf` and `#34d399`. A reviewer whose name hashes there gets green comment pins, ghost playheads and avatar rings meaning *this person*, applied as inline style from `Timeline.tsx:528/546` and `PeoplePanel.tsx:243`.

**Two colour bugs, free to take.** `TranscriptViewer.tsx:1677` renders the transcript error icon as `var(--color-warn, #f5a)`; `--color-warn` is defined nowhere (the tokens are `--warning` at `tokens.css:65` and `--color-warning` at `:126`), so it renders **hot pink** — and passes `css-var-contract` because a fallback counts as resolving. `.cp-review-reclabel` (`review.css:857`) is green text on a purple recbar while the mic button is red: three colours for one recording.

## 2. Current state — control vocabulary

**188 distinct classes appear on a `<button>`; 136 declare their own pressable surface. The shared system is 5 classes**: `.btn` (`buttons.css:9`), `.btn-primary` (`:53`), `.btn-ghost` (`:174`), `.btn-icon` (`:140`), `.btn.btn-compact` (`:215`). Of 488 `<button>` sites, 198 use the family, 230 use a bespoke class only, 60 carry no static class.

**Two files contain the same rule.** `.cp-icon-btn` (`transport.css:90-104`) and `.cp-queue-iconbtn` (`queue-drawer.css:389-402`) declare identical properties in identical order with identical hovers. Only the box size and a stray `padding: 0` differ.

**Fourteen classes do one job.** The close/remove "x" family drifts on six axes: sizes 18/18/18/19/20/20/20/20/24/24/26/28 (+one unsized), radius `--r-xs`×7 vs `--r-sm`×6, colour `--fg-3`×9 vs `--fg-4`×4, `background: transparent`×6 vs `none`×5 vs an rgba, glyph sizes across `--text-base` through `--text-3xl`. Seven of them are already pinned in `hit-target-contract.test.ts:40-47`'s `KNOWN_SMALL`.

**Seventeen pixel heights, no token.** 13/14/15/17/18/19/20/22/24/26/28/30/32/34/36/38/40 across 106 button-surface rules. `tokens.css` has `--r-*`, `--s-*`, `--text-*`, `--leading-*`, `--track-*`, `--weight-*`, `--z-*`, `--dur-*`, `--ease-*` — and **no control-height token of any kind**. `.btn` is 30px, `.btn-compact` 22px, and the most common values are 24 (19 rules), 26 (16), 22 (13), 30 (11).

**Icons have no colour token at all.** Every icon defaults to `stroke="currentColor"` (`Icons.tsx:15`), so its colour is whatever `color:` the container declares — 15 distinct values across 5 icon-button families that disagree on rest, active and disabled. `Icons.tsx:15` also declares `size = 16`, which is dead code: all 245 call sites pass an explicit size, across 20 distinct values with a mode of **13**. Disabled is never a colour; it is `opacity` at ten different values.

**One family already does what you want.** `.cp-icon-btn` (`transport.css:90-108`): `--fg-3` at rest → `rgba(255,255,255,0.05)` + `--fg-1` on hover → `--bg-5` fill + `--fg-0` when active. Zero green. Propagate it; do not invent a new pattern.

**Spacing is the biggest uncovered category.** Of 415 `gap` declarations, 91 use a token, 119 write a literal the `--s-*` scale already names, and 205 are off-scale — dominated by **6px (56)** and **10px (43)**, neither of which has a rung on a 4px scale starting at `--s-1: 4px` (`tokens.css:152-161`). Padding: 504 declarations, 74 tokenised.

**Uncovered leaks with cheap fixes.** 14 rules use the CSS keyword `ease`/`ease-out` instead of `var(--ease-out)` — a real curve change, `cubic-bezier(0,0,0.58,1)` vs `cubic-bezier(0.2,0.8,0.2,1)` (`tokens.css:272`). And duration literals survive because they are written in **seconds**: `buttons.css:257-261` has `0.35s` (= `--dur-slower`) and `0.12s` (= `--dur-fast`), invisible to every ms-shaped grep.

**Already locked down, do not re-propose:** radius, font-size, font-weight, line-height, letter-spacing, app z-index, `--font-mono`/tabular pairing, unreferenced tokens (`design-tokens-contract.test.ts`); literal hex duplicating a token (`token-usage-contract`); hex fallbacks on defined tokens (`token-fallback-contract`); `var()` resolvability (`css-var-contract`); `!important` ratchet; `cp-` prefix; dead classes; reduced motion; sub-24px targets; focus-ring colour.

## 3. Ranked worklist

### 1. Rebind the name — one line, 149 declarations move

`base.css:58-59` currently reads `--accent: var(--ella-green); --accent-rgb: 108, 255, 141;`.

Split it. Keep `--accent` as the role name and point it at a neutral (`var(--fg-2)` for text/glyph roles). Mint `--live: #6CFF8D` and `--live-rgb: 108,255,141` as the **only** green. Repoint the 67 group-(a) rules to `--live`; the other 48 follow the neutral automatically. Do this first so every later step is a deletion rather than a search.

### 2. Collapse seven green names to one

Delete `--ella-green` (`tokens.css:10`), `--success` (`:64`), `--color-accent-green` (`:123`), `--stroke-green` (`:53`), `--ring-green` (`:102`); repoint their references at `--live`.

**Ordering trap:** `design-tokens-contract.test.ts` fails on an unreferenced token and `css-var-contract` fails on an unresolved one, so each token's last reference and its definition must die in the **same commit**. `--ring-green` (1 ref, `settings.css:281`) and `--stroke-green` (1 ref, `shell.css:827`) are the cheapest starts.

### 3. Give icons a ladder; propagate the family that already works

Adopt `.cp-icon-btn` (`transport.css:90-108`) as the house rule and mint three tokens so the state has a name a test can read: `--icon-rest` (= `--fg-3`), `--icon-active` (= `--fg-0`), `--icon-disabled` — replacing ten different bare opacities.

Convert the five families that currently go green on active: `review.css:108` (`.cp-review-iconbtn.active`), `review.css:699` (`.cp-review-tool.active`), `transcript.css:59`, `settings.css:143`, `sidebar.css:514`. These are the icons you touch constantly, so this is where "brighter white and grey" actually reads.

### 4. Fix the review composer — both halves, differently

- `.cp-review-tool.active` (`review.css:699-703`) is real state → neutral active treatment from step 3 (`bg-5` fill, `fg-0` glyph). It also stops contradicting its own **red** `.recording` sibling at `review.css:705`.
- `.cp-review-drawhint` (`review.css:829-836`) is decoration → fully neutral (`fg-2` on `rgba(255,255,255,0.06)`, `--line-1` top border). The banner only mounts while drawing is armed, so its existence already carries the state — and the same class carries two non-drawing messages ("Transcribing your voice…", `dictNote`).
- `.cp-review-reclabel` (`review.css:857`) → red, matching the mic button and the recbar.

### 5. Neutralise all 25 group-(b) rules

Nothing here is state. In visibility order: the Fetch hover flare (`buttons.css:190-208` — see the decision in §5), the Generate hover glow (`buttons.css:298`, `:316`), the four resize rails (`resize.css:34/44/58/68` — these use the *same* green for `:hover` at 0.55 opacity and `.dragging` at 1.0, which is exactly the available-vs-live collision: keep drag green via `--live`, make hover white-alpha), the five native `accent-color` tints, the two AI citation styles.

### 6. Delete the 23 group-(c) greens, starting with the 15 that are free

`transport.css:194/209/516/565/578/588/590/613/623/635/655/674`, `review.css:651`, `room.css:237/247` need only `var(--accent)` → `var(--fg-3)` **inside the fallback slot**. Zero pixels change, because the fallback never fires. Then the genuine decoration: `welcome.css:16/33`, `loader.css:4`, `monitor.css:97` (a speaker name is identity, not state), `shell.css:824/827`, and the green radial stop in `--grad-brand-wash` (`tokens.css:132`).

### 7. Tokenise the grey fill layer

38 distinct white-alpha literals over ~305 declarations. The line layer got `--line-1/2/3` in r99; the fill layer never did — and it will get worse as green stops carrying state, because every neutralised rule reaches for another one-off alpha. Mint four fill tokens at the four dominant values — 0.03, 0.05, 0.08, 0.12 — absorbing the 0.015/0.02/0.025/0.035, 0.04/0.06, 0.07/0.09/0.10, 0.11/0.14 strays.

Collapse two near-duplicate greys while you are there: `--color-text-secondary` `#8A8A8E` (1 ref, `transcript.css:339`) into `--fg-4` `#86868F`, and `--stroke-1` `#141419` (1 ref, `settings.css:812`) into `--bg-2`/`--line-1`.

### 8. One-line bug

`TranscriptViewer.tsx:1677`: `var(--color-warn, #f5a)` → `var(--warning)`. Stops being the only pink pixel in the app.

### 9. Easing and duration literals

Fix the ~16 sites in §2. Treat the `ease-out` keyword sites as a **visual change**, not a rename — eyeball `transport.css:532/612` and `room.css:129`.

### 10. Icon size — enforce with the type system, not a test

Change `Icons.tsx:4` from `size?: number` to a union of the rungs you keep, and fix the dead default at `Icons.tsx:15` to the size actually dominant (13, not 16). `tsc` then fails all 268 call sites at once, which is the correct blast radius and needs no new contract file. Suggested rungs from the existing distribution: 11 / 13 / 16 / 20 / 28, with the four display sizes (32/44/52/96) split to a separate path.

### 11. Collapse the close/x family

14 classes → one `.cp-close` at 24px, which is also the WCAG 2.5.8 floor, so it retires seven entries from `hit-target-contract.test.ts:40-47` as a side effect. Delete `.cp-queue-iconbtn` (`queue-drawer.css:389`) and use `.cp-icon-btn` with a size modifier. Check `e2e/new-folder-consistency.spec.ts` (pins a class by name) and `e2e/card-unity.spec.ts` / `e2e/list-row-height.spec.ts` (measure geometry) before renaming.

### 12. Control-height scale — needs a decision first

Mint `--control-sm/md/lg`. 22/26/30 covers 59 of the ~106 height-declaring rules with no visual change; 32/34/36/38/40 is a fourth rung or a real redesign. Because the unreferenced-token contract fails on an unadopted token, mint and adopt in one commit.

### 13. Spacing — decide the two missing rungs before writing any test

6px and 10px are the r162 `--r-card` situation repeating verbatim: two clusters too large to be mistakes with nowhere to live. Either name them or round 99 declarations to 4/8/12 (a visible change). Once decided, enforce on `gap` **only** — it is single-valued and 119 declarations are exact token duplicates today. Leave `padding` alone: 404 of its 504 literals are two-axis shorthands where the axes rarely share a rung, and a mechanical sweep there is a regression generator.

**Not worth doing:** a box-shadow contract (the untokenised half is legitimately one-off glow stacks), a border contract (257 of ~300 `1px solid` already use `--line-*`), a centering-idiom contract (flex vs grid+place-items is style, not drift).

## 4. The new contract tests

This repo enforces design rules with tests. Three new ones, plus two amendments.

### A. `green-contract.test.ts` — the headline rule, written last

Write it **after** steps 1-2, because it is only tractable once exactly one green name exists.

- Scan every rule body in `src/styles/*.css` for `--live` / `--live-rgb`.
- Fail unless the rule's `file:selector` is on a pinned allowlist, seeded with the 67 group-(a) rules and **shrink-only** — the same ratchet `important-contract` uses and `hit-target-contract.test.ts:98-106` implements, including its honesty test at `:108` ("a class that grew past the minimum must leave the list; a stale entry reads as 'reviewed and accepted' when it means neither").
- **Canary required:** `expect(greenRules.length).toBeGreaterThan(0)` — CLAUDE.md:673 documents this as the failure this repo keeps shipping.

**Do not derive liveness from the selector.** `.active` means "playing now" at `transcript.css:508` and "the tab you happen to be looking at" at `settings.css:143`. A pattern test would bless both. This must be a **token** rule with a human-curated list, not a selector rule.

### B. `icon-color-contract.test.ts`

Once step 3 lands: every rule whose selector matches a known icon-button family must set `color` from `--icon-rest` / `--icon-active` / `--icon-disabled`, and must not express disabled as a bare `opacity`. Same ratchet shape, same canary.

### C. Extend `design-tokens-contract.test.ts`

Two new `describe()` blocks in the existing file rather than new files:

- **Transitions.** Reject any timing function that is not `linear` or a `var(--ease-*)`. Reject any duration literal equal to a `--dur-*` rung, **normalising seconds to ms first** — that normalisation is the entire point, since `buttons.css:257-261` hides four such literals behind `0.35s`/`0.12s`.
- **Control height** (after step 12) and **gap** (after step 13): the radius-shaped "never writes a literal a token already names" check. Verify the `:root`-only premise for `--s-*` first, exactly as the radius block does for `--r-*`.

### D. Amend `focus-contract.test.ts` — it has a live hole

`focus-contract.test.ts:17-18`:

```js
const GREEN =
  /--accent\b|--accent-rgb|--ella-green|--color-accent-green|108,\s*255,\s*141|#6cff8d|#88f362/i;
```

It omits `--success`, `--ring-green` and `--stroke-green`. A `:focus` rule painting the identical hex through any of those three names passes today. No rule does yet — latent, not live — but a green-neutralisation pass could walk straight into it. After step 2, reduce the regex to the single surviving `--live` name and drop the stale `#88f362`, which exists nowhere in `src/` except that line.

### E. Register every new test

`contract-register.test.ts` asserts that CLAUDE.md's spelled-out count matches the table's row count and that every named test file exists. Each new contract needs: the test file, a row in the table at `CLAUDE.md:694+`, and `CLAUDE.md:659` bumped from "Fifty-nine".

## 5. Disagreements, and decisions only you can make

**Between the passes:** pass 3 (colour) and pass 4 (sizing/vocabulary) do not contradict each other — they cover disjoint categories and their overlapping facts agree (both record `.btn-primary` as already neutral). Pass 3's raw counts differ slightly from mine (`--ella-green` 15 vs 14, `--color-accent-green` 22 vs 20) because pass 3 counted across `src/` including `.tsx` and I counted `src/styles/*.css`. Not a disagreement. Group assignment of the 115 rules is pass 3's judgement, not a measurement; the genuinely debatable ones are the 18 success/outcome rules, the four `.active`-that-means-selected rules (`settings.css:143/277/318`, `palette.css:115`), and the four resize handles, which are legitimately group (b) on hover and group (a) while dragging **from the same declaration**.

**Between pass 3 and the repo:** pass 3 recommends neutralising the Fetch hover flare. `buttons.css:186-189` documents it as a deliberate exception under a *different* stated rule — "green marks the primary action and live states" — which commit `8071823` supersedes without deleting. That contradiction is unresolved in the tree and only you can settle it.

**Decisions:**

1. **Does green mark success, or only liveness?** 18 of the 67 group-(a) rules mean "finished / passed / approved", not "happening now". Strip them and the app loses its only success semantic; `.cp-status-chip.approved` (`review.css:907`) becomes an ordinary chip. Keep them and "green means live" is not literally true. A third option — a separate `--ok` token — costs one more name.
2. **Does Fetch keep its flare?** `buttons.css:186-189` says it is the only toolbar button that flares, precisely so it reads apart from Clear and Import. Neutralise it and the toolbar's primary action becomes visually identical to its neighbours; the fix is to give it the `.btn-primary` raised-neutral shape, which is what that pattern is for.
3. **"Controls are grey" — how grey?** There is **no light-grey surface token**. `--bg-5` `#403F46` (`tokens.css:19`) is the lightest surface the palette owns, and `.btn-primary` already uses `bg-4`/`bg-5`. A genuinely *light* grey chip on a `#0E0E10` canvas needs a new palette step and a re-decision of the raised-control language — the bevel insets at `tokens.css:86-96` are tuned for dark fills and will look wrong on a light one. Confirm which you mean: "lighter than the green" (already true) or "a light grey chip" (a redesign).
4. **Do `AVATAR_COLORS` lose their two greens?** `review.ts:789` contains `#2dd4bf` and `#34d399`. "Green means live" is not actually true until they go — and removing them changes existing users' assigned colours, which no CSS-scanning contract can see or enforce.
5. **Two new spacing rungs, or 99 rounded declarations?** `--s-1-5`/`--s-2-5` makes the scale 12 rungs and weakens its authority. Rounding is a visible redesign that should be reviewed on screen, not merged as a cleanup.
6. **Contrast risk you should price in now.** `#6CFF8D` reads ~14:1 on `--bg-1`; `--fg-4` `#86868F` reads 5.19:1 and `--fg-3` `#A2A0AA` 7.48:1. Every green *label* turned grey loses 2-3x luminance, and the ones sitting on their own tinted fill (`review.css:830`, `settings.css:320`, the queue status pill) are where it drops under 4.5:1. `tokens.css:41-48` records that exactly this class of miss was found only by pointing the sweep at a state it had never reached. Run `e2e/contrast.spec.ts` **and** `e2e/deep-state-contrast.spec.ts` after every batch, not at the end. Batch by **group** — all of (c) first, 15 of which are free no-op edits, then (b), then (a) — never by file, so each commit is one reviewable claim about meaning and can be reverted alone.

---

## Verified by hand, 2026-08-25

Added after a sweep prompted by a report of a square corner in an icon
overlay. The reported corner was NOT reproduced - see below - but the sweep
turned up one real inconsistency and confirmed several things are sound.

### The icon set has eight corner radii and no scale

`grep -oE 'rx="[0-9.]+"' src/components/Icons.tsx | sort | uniq -c`:

| rx | count |
|----|-------|
| 1 | 6 |
| 2 | 5 |
| 1.5 | 3 |
| 3 | 2 |
| 0.5 | 2 |
| 8 | 1 |
| 2.5 | 1 |
| 2.25 | 1 |

Eight values, chosen per icon. `2.25` and `2.5` differ by a quarter pixel at
a 24-unit viewBox and cannot be told apart at any size the app renders. The
radius tokens in `tokens.css` exist precisely so this kind of thing is picked
from a scale; the icon set does not use them because SVG `rx` is in viewBox
units rather than CSS pixels, so a token would need a viewBox-relative
counterpart. Worth one: three values (a hairline, a normal, a pill) would
cover every icon in the set.

No `<rect>` in the set is missing `rx` entirely, so nothing is fully sharp.

### Checked and sound

- Every control in the transcripts picker header computes a 6px radius:
  count pill 999px (a deliberate pill), view toggle, New project, search and
  sort all 6px.
- `.cp-annot-tool` (the drawing overlay's tool buttons) - 6px.
- `.cp-reader-row-thumb` - 6px with `overflow: hidden`, so its poster is
  clipped rather than overflowing square.
- A sweep of every `button`, `[role=button]`, `[role=radio]` and `[role=tab]`
  across the Library, Transcripts and Clip views, filtered to those with a
  visible background or border, found **zero** with a computed radius of 0.

### The four `border-radius: 0` rules are all deliberate

`.cp-lib-card-art` (the card does the rounding now), `.cp-lib-lrow` (a list
row is not a card), the popped-out queue drawer (it fills its own OS window),
and a 12px resize handle. None is an icon overlay.

---

# DOCUMENT 3 — What the 2026-09-02 audit left open

A seven-dimension audit (tokens, elevation/radii/motion, colour semantics,
typography, lists/selection, popovers/dialogs, buttons/controls) found 101
findings. Twenty-six wrong STATES were fixed in the commits after `8c7566f`,
each with a contract (`elevation`, `danger-text`, `pane-empty`,
`inline-typography`, plus new blocks in `green` and `list-zebra`). The
77 below are still open. Each carries the evidence the auditor measured
and the fix it proposed; treat the fix as a proposal, verify the evidence
against HEAD first (line numbers drift), and keep the register in CLAUDE.md
in step if a fix adds a contract. Severity is the auditor's.

### Findings

- **[medium] Transition durations retyped as literals that a token already holds, in CSS and in JS**
  .cp-gen-btn (buttons.css:243-247) transitions border-color, box-shadow and background at `0.35s` (= --dur-slower, which the same file uses at :286,:306,:428) and transform at `0.12s` (= --dur-fast). .cp-screenings-chev (coreview.css:585) uses `transform 120ms ease` (= --dur-fast). QueueDrawer.tsx:637 and :655 set an inline `transition: "transform 180ms cubic-bezier(0.2, 0.8, 0.2, 1)"`, which is --dur-base + --ease-out spelled out in a second language; a retune of either token leaves the tab-drag settle on the old curve. No contract forbids a numeric duration inside a `transition` value; design-tokens-contract only asserts each token is referenced somewhere.
  Evidence: src/styles/buttons.css:243-247; src/styles/coreview.css:585; src/components/QueueDrawer.tsx:637,655; src/lib/design-tokens-contract.test.ts:122-124 (only mention of --dur)
  Fix: buttons.css:243-247 -> var(--dur-slower)/var(--dur-fast); coreview.css:585 -> `transform var(--dur-fast) var(--ease-out)`. In QueueDrawer, move the settle transition to a `.cp-tab` rule in queue-drawer.css (`transition: transform var(--dur-base) var(--ease-out)`) and have tabTransformStyle emit only `transition: "none"` for the dragged tab. Add to design-tokens-contract: a `transition` value containing `\d+(ms|s)` fails unless the selector is one of the three library.css hero rules, listed by name.
- **[medium] Easing keywords bypass the --ease-* set, and DESIGN.md names a token that does not exist**
  docs/DESIGN.md:189 lists `--ease-linear` for progress; tokens.css defines --ease-out, --ease-in-out and --ease-spring only (grep for ease-linear across src/ hits nothing). Meanwhile 16 transitions use a bare keyword: `ease` at ai.css:120,132, monitor.css:196, transcript.css:119,669,1083,1488, coreview.css:585; `ease-out` at room.css:143, transport.css:562,618; `linear` at ai.css:76, coreview.css:389, monitor.css:1161, settings.css:415, transport.css:214. Browser `ease` is a different curve from --ease-out (0.2,0.8,0.2,1), so a chip hover and a button hover decelerate differently. transcript.css:1515 animates .cp-colorpick with `cubic-bezier(0.16, 1, 0.3, 1)`, which is --ease-spring (0.22, 1, 0.36, 1) retyped by eye.
  Evidence: docs/DESIGN.md:189; src/styles/tokens.css:296-297,320 (the three easings defined); src/styles/ai.css:76,120,132; src/styles/transcript.css:119,669,1083,1488,1515; src/styles/monitor.css:196,1161; src/styles/transport.css:214,562,618; src/styles/room.css:143; src/styles/coreview.css:389,585; src/styles/settings.css:415
  Fix: Add `--ease-linear: linear;` to tokens.css and use it at the five progress sites (that also satisfies the unreferenced-token check). Replace the eight `ease` and three `ease-out` keywords with var(--ease-out). transcript.css:1515 -> var(--ease-spring). Extend the transition contract: the easing slot of a transition must be var(--ease-*).
- **[medium] --dur-instant's documented role (hover tints) is not what the app does with it**
  tokens.css:314 and DESIGN.md:184 define --dur-instant (80ms) as 'hover tints'. Counting the property each token animates: background tints run at --dur-fast in 70 declarations and at --dur-instant in 2 (ai.css:120, transcript.css:119); color at --dur-fast 48 vs --dur-instant 1. --dur-instant has eight uses in total, mostly transform pops (.cp-tx-speaker scale, .cp-ai-chip). One control goes the other way: .cp-url tints background/border/box-shadow at --dur-base (toolbar.css:28-31), slower than every other field. The doc and the codebase disagree about which rung is 'hover', so a new contributor following the doc will write the minority value.
  Evidence: src/styles/tokens.css:314-315; docs/DESIGN.md:184; src/styles/ai.css:120; src/styles/transcript.css:119; src/styles/toolbar.css:28-31; property-by-token counts from grep over src/styles
  Fix: Re-document rather than move 70 rules: tokens.css:314 and DESIGN.md:184 -> `--dur-instant: micro-feedback that must not lag the pointer (a chip pop, a swatch scale)`, `--dur-fast: hover and focus tints, small control state`. Move toolbar.css:28-31 to --dur-fast so the URL field matches every other input.
- **[medium] Disclosure chevrons rotate at two speeds**
  The same interaction, a chevron turning on expand/collapse, transitions transform at --dur-base in sidebar.css:50 (.cp-section-head .chev), logs.css:35 (.cp-logs-header .chev) and room.css:257 (.cp-people-collapse), at --dur-fast in shell.css:1006 (.cp-reader-group-chev), transcript.css:1801 (.cp-cast-chev), :1898 (.cp-tx-group-chev) and ai.css:256 (.cp-ai-caret), and at a literal `120ms ease` in coreview.css:585. tokens.css:315 assigns 'small state changes on a control' to --dur-fast.
  Evidence: src/styles/sidebar.css:50; src/styles/logs.css:35; src/styles/room.css:257; src/styles/shell.css:1006; src/styles/transcript.css:1801,1898; src/styles/ai.css:256; src/styles/coreview.css:585
  Fix: Set all eight to `transform var(--dur-fast) var(--ease-out)`. Optionally define a shared `.cp-chev` rule in base.css and drop the seven copies.
- **[medium] Shared entrance keyframes live in one component file and are consumed by six others, with nothing checking the name resolves**
  cp-fade-in and cp-modal-in are defined in settings.css:31-38 and used by library.css (:642,:746), palette.css (:20), monitor.css (:430,:486,:568,:771,:851,:997), toolbar.css (:104) and transcript.css (:1062,:1137,:1152). cp-palette-in is defined in palette.css:37 and used by library.css:758. Keyframes are global, so it renders, but a rename or deletion in settings.css silently removes fifteen popover entrances elsewhere: an animation naming a missing keyframe is not an error. css-var-contract resolves var() only; no test resolves animation-name to an @keyframes.
  Evidence: src/styles/settings.css:31-38; src/styles/palette.css:37; consumers listed from grep -lE 'animation.*cp-fade-in|cp-modal-in|cp-palette-in' src/styles
  Fix: Move cp-fade-in, cp-modal-in and cp-palette-in into base.css under a 'SHARED ENTRANCES' block (base.css already hosts the shared keycap, swatch and pane-empty rules). Extend css-var-contract with a second block: every `animation`/`animation-name` in src/styles names an @keyframes that exists, with a canary that it found > 30 uses.
- **[medium] A wrong fallback on a defined motion token, and seven radius fallbacks, escape token-fallback-contract because it only matches hex**
  transcript.css:1062 writes `cp-fade-in var(--dur-fast, 140ms) var(--ease-out, ease)`; --dur-fast is 120ms and --ease-out is a cubic-bezier, so both fallbacks describe values the app does not use. This is exactly the drift token-fallback-contract was written to end (its header: 'thirty-four fallbacks were left holding the OLD value'), but its regex is `var\(\s*(--[a-z0-9-]+)\s*,\s*(#[0-9A-Fa-f]{3,8})` and sees only colours. Seven more: `var(--r-md, 8px)` at coreview.css:165,423, review.css:981, room.css:464,603,787,815. The 5b9c7b0 commit message says these fallbacks were stripped; the non-hex ones were not.
  Evidence: src/styles/transcript.css:1062; src/lib/token-fallback-contract.test.ts:45 (hex-only regex); src/styles/coreview.css:165,423; src/styles/review.css:981; src/styles/room.css:464,603,787,815
  Fix: Strip the eight fallbacks. Change the contract regex to `var\(\s*(--[a-z0-9-]+)\s*,\s*([^)]+)\)` and keep the existing exemption for tokens tokens.css does not define.
- **[medium] Bevel insets are hand-rolled at fourteen sites with eight different highlight alphas, beside three --bevel-* tokens**
  tokens.css:83-97 provides --bevel-raised/-hover/-pressed, used by the .btn family (buttons.css:34-171). Outside that, the same 'inset 0 1px 0 white' top-edge highlight is retyped at alpha 0.06 (palette.css:131, sidebar.css:563), 0.08 (palette.css:29,215, queue-drawer.css:182), 0.10 (sidebar.css:509, settings.css:752, transport.css:329), 0.30/0.35 (buttons.css:100,118 .cp-export-cta), 0.4/0.6 (sidebar.css:429,461 toggle thumb); the bottom shade `inset 0 -1px 0 black` at 0.20 (sidebar.css:510), 0.22 (buttons.css:101,119), 0.25 (palette.css:178,301, settings.css:949). .cp-gen-btn (buttons.css:239-242) carries a private three-layer bevel next to .btn's tokenised one. The CTA glows are legitimately bespoke; the glass top-edge on palette/queue/sidebar surfaces is one idea written five ways.
  Evidence: src/styles/tokens.css:83-97; src/styles/palette.css:29,131,178,215,301; src/styles/queue-drawer.css:182; src/styles/sidebar.css:509-510,563; src/styles/settings.css:752,949; src/styles/transport.css:329; src/styles/buttons.css:100-101,118-119,239-242
  Fix: Add `--bevel-hair: inset 0 1px 0 rgba(255,255,255,0.08)` (the top-edge glass line) and `--bevel-base: inset 0 -1px 0 rgba(0,0,0,0.25)` to tokens.css; use them at the palette, queue-drawer, sidebar and settings sites. Rebuild .cp-gen-btn's resting shadow as `var(--bevel-raised)` plus its green edge layer. Leave .cp-export-cta and the toggle thumb bespoke with a one-line comment each.
- **[medium] The swatch 'picked' ring exists in five variants although base.css declares ColorSwatches the one swatch language**
  base.css:198-200 says .cp-swatches is 'THE swatch language for every color choice' and draws the picked ring as `0 0 0 2px var(--bg-1), 0 0 0 3px var(--fg-1)` (base.css:239). Elsewhere the same state is: .cp-tagrow-dot.on `0 0 0 2px var(--bg-1), 0 0 0 3.5px rgba(255,255,255,0.92)` (library.css:1634); .cp-tx-rename-icon.picked `0 0 0 2px rgba(255,255,255,0.85)` (transcript.css:2060); .cp-badgesheet-cell.picked `inset 0 0 0 1.5px rgba(255,255,255,0.85)` (transcript.css:2161); .cp-tx-speaker.renamed `0 0 0 2px var(--bg-0), 0 0 0 3px var(--fg-4)` (transcript.css:672). Ring widths 1.5/2/3/3.5px and whites 0.85/0.92/fg-1 for one meaning.
  Evidence: src/styles/base.css:198-200,239; src/styles/library.css:1634; src/styles/transcript.css:672,2060,2161
  Fix: Add `--ring-picked: 0 0 0 2px var(--bg-1), 0 0 0 3px var(--fg-1)` to tokens.css beside --ring-selected, use it at base.css:239, library.css:1634, transcript.css:2060; make .cp-badgesheet-cell.picked and .cp-tx-speaker.renamed use it too unless a comment says why the gap colour must differ.
- **[medium] The global focus ring is 1px but twenty-five components override it to 2px**
  base.css:126-131 draws focus as `box-shadow: 0 0 0 1px var(--focus-ring)` for button/input/[role=slider]/[role=button]. Component rules then redraw it: `0 0 0 2px var(--focus-ring)` at 25 sites (library.css:2112,1635; monitor.css:1213,1405; review.css:1140,1279,1334; settings.css:1349,1400; shell.css:997,1027,1045; sidebar.css:570; transcript.css:756,1234,1281,1452,1747,1794,1893,1970,2013,2062,2163,2266), `outline: 2px solid` at 7 (base.css:234, coreview.css:685,738, library.css:2559, room.css:1012,1076, transport.css:750), `inset 0 0 0 1px` at 3 (library.css:979,2012,2091), and a 2px-gap + 3px ring at 3 (transcript.css:1496,1520,1524). focus-contract checks colour only; e2e/focus-visible checks that the focused state differs from resting, not its weight. The result is a keyboard user seeing the ring thicken and thin as they tab between a toolbar button and a transcript control.
  Evidence: src/styles/base.css:126-131; 25 `0 0 0 2px var(--focus-ring)` sites (grep count); src/lib/focus-contract.test.ts (colour only); e2e/focus-visible.spec.ts:30
  Fix: Change base.css:130 to `0 0 0 2px var(--focus-ring)` (the majority value) and delete the 25 identical overrides; keep the inset and offset variants where a comment explains the clipping reason. Add one line to focus-contract: a component-level focus ring must be var(--focus-ring) at 2px unless inset.
- **[low] Radius longhands and per-corner shorthands retype scale values the contract cannot see**
  The 'radius scale is complete' block matches `/border-radius\s*:/`, so `border-top-left-radius: 6px; border-top-right-radius: 6px` on .cp-tab (queue-drawer.css:152-153) is invisible to it while being exactly --r-sm. Per-corner shorthands are exempted as 'shape' (design-tokens-contract:389, DESIGN.md:150), but their values are still scale values: `0 0 6px 6px` (review.css:294,1301), `0 2px 2px 0` (nav.css:99), `0 0 1px 1px` (transport.css:479).
  Evidence: src/styles/queue-drawer.css:152-153; src/styles/review.css:294,1301; src/styles/nav.css:99; src/lib/design-tokens-contract.test.ts:387 (regex)
  Fix: queue-drawer.css:152-153 -> var(--r-sm); review.css:294,1301 -> `0 0 var(--r-sm) var(--r-sm)`; nav.css:99 -> `0 var(--r-2xs) var(--r-2xs) 0`. Widen the contract regex to `border(-top|-bottom)?(-left|-right)?-radius` and, for shorthands, check each px component against the scale.
- **[low] `@keyframes scan` is the only unprefixed keyframe; the prefix contract does not cover keyframe names**
  38 of 39 keyframes are cp-* (cp-fade-in, cp-modal-in, cp-toast-in ...). monitor.css:635 defines `@keyframes scan`, used by .cp-scanline::before at :633. class-prefix-contract scans class selectors only, so a generic keyframe name that a future dependency's stylesheet could collide with passes.
  Evidence: src/styles/monitor.css:633-638; src/lib/class-prefix-contract.test.ts (no keyframe handling)
  Fix: Rename to cp-scan at both lines; add a two-line check to class-prefix-contract that every `@keyframes` name starts with `cp-`.
- **[low] CommandPalette comments describe a close animation that does not exist**
  CommandPalette.tsx:113-114: 'Defer so the close animation can start before we hand control to the run handler'. The component returns null at :123 when !open, and palette.css defines only cp-palette-in (:37); there is no exit keyframe or closing class anywhere in src/styles. The rAF defer is still useful (it lets the palette unmount before a command opens another modal) but the stated reason is wrong and invites someone to 'fix' the missing animation.
  Evidence: src/components/CommandPalette.tsx:113-116,123; src/styles/palette.css:35-45
  Fix: Rewrite the comment: 'Defer one frame so the palette has unmounted before the command opens another modal or notification, which would otherwise mount underneath it.'
- **[low] Elevation has no section in DESIGN.md; the shadow roles exist only as comments in tokens.css**
  DESIGN.md's headings are Type, Colour, Spacing, Radii, Stacking, Motion, Targets, Voice, Naming, What checks this. The only mention of shadows is :76 ('pick by depth'). tokens.css:78-97 carries the actual policy (floating layers get --shadow-card; in-flow chrome gets lines, not blur; bevels for buttons and cards) and it is the file DESIGN.md says a reader should not have to open. Findings 2, 3, 11 and 12 above are what a policy that lives only in a token comment produces.
  Evidence: docs/DESIGN.md headings (grep '^## '); src/styles/tokens.css:78-97
  Fix: Add `## Elevation` to DESIGN.md between Radii and Stacking with a five-row table: in-flow chrome = 1px --line-*, no shadow; control = --bevel-raised/-hover/-pressed; floating (popover, menu, toast, card lift) = --shadow-card; dialog = --shadow-modal; rings = --focus-ring 2px, --ring-selected, --ring-picked. Register the new shadow-role contract in the 'What checks this' table.

### Findings

- **[high] 'This job is running' is drawn in four colours on surfaces that share the main window** *(partly done: done is green now; running stays neutral in the queue, gold on the track, orange in the logs header - one running colour still wants a decision)*
  Generate button running = green sweep (buttons.css:385-403, user call 2026-08-29, reasoning: 'a job that is actually running is the same category as the session dot'). Queue drawer running = neutral white (queue-drawer.css:303-305 .cp-queue-item.running, :358 .cp-queue-status.running use --accent), and .done uses the identical white (:307-309, :359), so running and finished are indistinguishable by colour in the queue list. Track queued/running range = gold (transport.css:374 default --range-c: var(--gold); :417 .running only adds a pulse). Logs header working pill = orange --warning (logs.css:53). Queue drawer, track and logs panel are co-visible; the contract's own reasoning was applied to one button only.
  Evidence: src/styles/buttons.css:390-403; src/styles/queue-drawer.css:303-309, 358-359; src/styles/transport.css:373-374, 417; src/styles/logs.css:53
  Fix: Pick one 'running' colour and make queued/done distinct from it. Minimum: logs.css:53 .status-pill.working off --warning (in-flight is not a warning) onto --accent or --ella-green; queue-drawer.css:358 .cp-queue-status.running and :303 .cp-queue-item.running onto the same choice; if green, add "queue-drawer.css": ["cp-queue-status", "cp-queue-item"] and "logs.css": ["status-pill"] to green-contract ALLOWED under 'work actually running'. Keep gold for QUEUED (not started) only, so gold/green/white/red on the track and in the drawer read queued/running/done/failed the same way.
- **[medium] docs/DESIGN.md's Colour section describes the pre-08-26 system and contradicts base.css and green-contract**
  DESIGN.md:66-68: '--accent (--ella-green) means one of exactly two things: this is the primary action, or this is a notification.' base.css:58-81: --accent is var(--fg-1), NEUTRAL; green = positive outcome + live. green-contract.test.ts:18-63 lists eight green categories. DESIGN.md:88-90 recommends color-mix(var(--accent) 20%) as 'a translucent brand colour' - that now yields a white tint. DESIGN.md:96-97 says --color-accent-green 'beats both its older synonyms in usage'; today --ella-green 19, --success 9, --color-accent-green 3. buttons.css:88-91 still says the export CTA 'overrides the green .btn-primary' while tokens.css:97-99 says .btn-primary is a raised neutral. git: DESIGN.md last touched 3bb26be (2026-08-23); base.css de-greened in 9079034 (08-26) with four later colour commits (bed5016, 5b3d43f, d31e5db, bd4183a). CLAUDE.md names DESIGN.md 'the design system reference ... Read it before adding a style', so a new contributor is told to do the thing the contract fails.
  Evidence: docs/DESIGN.md:66-68, 88-90, 96-97; src/styles/base.css:58-81; src/lib/green-contract.test.ts:18-63; src/styles/buttons.css:88-91; src/styles/tokens.css:97-99
  Fix: Rewrite DESIGN.md Colour: '--accent is neutral (--fg-1); emphasis is brightness. Green means exactly what green-contract.test.ts ALLOWED lists (link it as the register). Violet = primary action, content selection, in/out marks. Warning/danger rules per the token comments.' Replace the color-mix example with var(--ella-green). Fix buttons.css:88-91. Add a docs-contract assertion that DESIGN.md does not pair --accent with --ella-green (grep 'accent.*ella-green').
- **[medium] Gold carries three meanings, two of them on the room screen, while the token says it has no status meaning**
  tokens.css:74 '--gold: host crown / winner accent - no status meaning'; DESIGN.md:113-114 repeats it. But review.css:938-941 .cp-status-chip.changes (a verdict) is gold, and transport.css:374 makes gold the queued range's --range-c under a comment calling it 'a status color system'. In a session App.tsx:4586 (PeoplePanel, gold crown room.css:361) and App.tsx:4663 (ReviewStatusChip inside .cp-room-title at :4654) render together, so gold = 'host' and gold = 'changes requested' on one header.
  Evidence: src/styles/tokens.css:74; docs/DESIGN.md:113-114; src/styles/review.css:938-941; src/styles/transport.css:373-374; src/styles/room.css:361; src/App.tsx:4586, 4654-4663
  Fix: Move 'changes requested' (review.css:938-941) and the queued range default (transport.css:374) to the warning family - both are 'needs your attention' states - leaving gold to identity only, which makes the tokens.css:74 claim true. If gold is wanted for verdicts, delete the 'no status meaning' sentence in both files instead.
- **[medium] Two warning tokens with different hues, and the stalled --color-* aliases the token file says were removed are still live**
  tokens.css:65 --warning #E87826 (orange) and :156 --color-warning #FFB545 (amber) are not aliases; they are two colours. .warn classes split between them: sidebar.css:230-235 .cp-source-hint.warn uses --warning (its comment says 'Same amber as the log panel's warn tag' - it is orange), settings.css:599-604 .cp-mediainfo-badge.warn uses --color-warning, coreview.css:392 meter yellow uses --color-warning. tokens.css:114-117 says unadopted --color-* aliases 'have been removed'; remaining: --color-warning (5), --color-destructive (2), --color-accent-purple (2, same hex as --novella-violet), --color-text-secondary (1). Two of the four are DIFFERENT hexes from the meaning's other name, so a retune of --warning leaves the mediainfo badge behind.
  Evidence: src/styles/tokens.css:65, 114-117, 154-156; src/styles/sidebar.css:230-235; src/styles/settings.css:599-604; src/styles/coreview.css:392; src/styles/shell.css:830-831
  Fix: Delete --color-warning, --color-destructive, --color-accent-purple, --color-text-secondary. Map: --color-warning -> --warning (5 sites; if the meter's yellow zone genuinely wants amber, give it a meter-local custom prop in coreview.css); --color-destructive -> --danger-text; --color-accent-purple -> --novella-violet; --color-text-secondary -> the --fg-* step it matches. Correct the tokens.css:114-117 sentence and the sidebar.css:230 'amber' comment. design-tokens-contract's unreferenced-token check will then hold the line.
- **[medium] Reviewer identity colours overlap the track's status colours on the same track**
  review.ts:956 AVATAR_COLORS = [#4dabf7, #f5a623, #7b61ff, #2dd4bf, #ff6b6b, #34d399, #e879f9, #fb7185]; avatarColor() hashes a name into it; markers.ts:109 and Timeline.tsx:528/546 push it into --marker-color; transport.css:532 .cp-track-comment paints the pin with it. The same track carries .cp-track-queued ranges in gold/green/red (transport.css:374, 415, 416) and the in/out range in violet (:315-325). A reviewer hashed to #34d399 draws green pins beside a green 'exported' range; #ff6b6b red pins beside a red 'failed' range; #7b61ff sits a few degrees off the in/out violet; #f5a623 is near gold. Hue is the pin's only channel, so identity and status share pixels.
  Evidence: src/lib/review.ts:956-962; src/lib/markers.ts:109; src/components/Timeline.tsx:528, 546; src/styles/transport.css:315-325, 374, 415-416, 532
  Fix: Keep AVATAR_COLORS at length 8 (avatarColor is hash mod length; changing the length reshuffles every existing reviewer) but swap the four status-adjacent entries in place for hues the track never uses (e.g. cyan, sky, lilac far from #6D52ED, coral-pink). Alternatively give pins a second channel (a white inner ring) so status ranges and pins can never be confused by colour alone.
- **[medium] 'Selected' has two visual languages and tokens.css documents both as the rule**
  tokens.css:100-108: --ring-selected 'used on sidebar items, toggles ON, queue currently-exporting ... reads as lifted'; base.css:65-70: 'a selected row ... read as lifted rather than as coloured'. tokens.css:125-141: selection is 'A HUE, not another rung on the neutral ladder' -> --sel-fill violet. In code: library rows/cards/tree are violet (library.css:888-889, 1045, 1344); settings model row is a neutral ring (settings.css:286-288, the ONLY use of --ring-selected) and its 'selected' badge is neutral (settings.css:327). The distinction (content vs. option) is real and defensible, but nowhere written, so the next selected surface picks by file proximity.
  Evidence: src/styles/tokens.css:100-108, 125-141; src/styles/base.css:65-70; src/styles/library.css:888-889, 1045, 1344; src/styles/settings.css:286-288, 327
  Fix: Write it once, beside --sel-fill: 'violet = selection of CONTENT you will act on (rows, cards, tree, in/out range); the neutral --ring-selected = a control or option currently chosen (model row, toggles)'. Update base.css:65-70 and tokens.css:100-108 to say the same. Since --ring-selected has one use, either adopt it as the named 'chosen option' ring or inline it in settings.css:288 and delete the token.
- **[medium] Violet is action, selection, in/out marks, a transport caveat label and a drop category, and the aliases meant to separate them all resolve to one hex**
  tokens.css:137-140 admits '--novella-violet is already the primary button and --marker' and aliases --sel-fill 'so a selection that has to be retuned should not drag a button and every marker with it' - but nothing has been retuned, so today the export CTA (buttons.css:95/114 gradient, glow :104/122/131), the in/out range and marks (transport.css:315-325, 446, 463 via --marker = base.css:88), the reader pins (shell.css:606-663), library selection (library.css:888, 1045, 1344) are one colour. Two more meanings ride the same hue: room.css:1113 .cp-peerstage-badge ('Live view of X's screen', PeerStageVideo.tsx:47-49) borders a caveat label in violet, and shell.css:830-831 .cp-drop-card.transcript uses it as a file-type category via --color-accent-purple. On the main view the sidebar CTA, the track range and the reader pins are co-visible; in a session the caveat badge joins them.
  Evidence: src/styles/tokens.css:137-145; src/styles/base.css:88; src/styles/buttons.css:95, 104, 114; src/styles/transport.css:315-325; src/styles/library.css:888, 1344; src/styles/room.css:1113; src/styles/shell.css:830-831
  Fix: Make the aliases do the job their comment claims: give --sel-fill its own value (a lighter, less saturated violet, e.g. color-mix(in srgb, var(--novella-violet) 70%, var(--bg-5)) or a distinct hex) so selection, action and marks are three tones. Change .cp-peerstage-badge (room.css:1113) to border-color: var(--line-3) - a caveat is not an action. Point .cp-drop-card.transcript at --novella-violet and delete --color-accent-purple.
- **[medium] The same red says 'microphone is OFF' in the room bar and 'microphone is HOT' in the review composer**
  device-state-contract pins .cp-room-bar-btn.off / .cp-person-ctl.off / .cp-person-muted to --danger-text (room.css:364, 438). review.css:706-712 .cp-review-tool.recording and :841-844 .cp-review-drawhint.recording also use --danger-text (with rgba(255,107,107) tints) for a live dictation mic. During a co-review session the room bar and the Review drawer are both on screen, so one red means 'off' 20px from where it means 'on'; the pulse is the only difference. green-contract's own 'LIVE broadcast' category (the session dot, the mic meter's good zone) is the category a hot mic belongs to.
  Evidence: src/styles/room.css:364, 438; src/styles/review.css:706-712, 841-844; src/lib/device-state-contract.test.ts:31-36; src/lib/green-contract.test.ts:20
  Fix: Draw recording as a live state: .cp-review-tool.recording and .cp-review-drawhint.recording -> color: var(--ella-green) with a color-mix(var(--ella-green) 16%) background, keep the pulse, and add "review.css": [..., "cp-review-tool", "cp-review-drawhint"] to green-contract ALLOWED under live broadcast. If recording-red is a deliberate broadcast convention, say so in device-state-contract's header so the next reader does not 'fix' either side.
- **[low] Three live dots, three drawings, two token names**
  nav.css:107-116 .cp-nav-badge: 7px --ella-green, 2px bg ring, WHITE glow (--accent), 2s pulse. coreview.css:191-199 .cp-colobby-live: 9px --ella-green, 3px white ring + white glow (--accent-rgb), 2s pulse. room.css:72-77 .cp-room-live: 8px --success, no ring, 2.4s pulse. --success for a live state is a naming mismatch (live is not an outcome). The nav badge and the room dot are on screen together during a session.
  Evidence: src/styles/nav.css:107-116; src/styles/coreview.css:191-199; src/styles/room.css:72-77
  Fix: One .cp-live-dot in base.css (size via --live-dot-size, background var(--ella-green), one ring, one pulse, one reduced-motion rule); the three sites add the class and drop their own declarations. Collapse the three green-contract ALLOWED entries into "base.css": ["cp-live-dot"].
- **[low] Stale comments describe colour semantics the code no longer has**
  base.css:83-88 (--marker) cites 'the running-queue-item pulse' (no queue rule references --marker), 'the green accent on focus' and 'success-green CTAs'. buttons.css:88-91: 'Overrides the green .btn-primary' (neutral per tokens.css:97-99). room.css:945-948 computes contrast for 'accent green #6CFF8D' on a badge whose color is var(--accent) = white. finder-tags.ts:29-31: accent '#6CFF8D, which everywhere else means selected'. review.ts:953-955: '#4dabf7 - the same hue as the timecode chips' (now green). sidebar.css:230-231: 'Same amber as the log panel's warn tag' (it is orange --warning). tokens.css:114-117: unreferenced --color-* aliases 'have been removed' (four remain). Each is the kind of prose that made this audit's first hour slower, and the repo's own rule is that a comment is corrected in the commit that changes the code it describes.
  Evidence: src/styles/base.css:83-88; src/styles/buttons.css:88-91; src/styles/room.css:945-948; src/lib/finder-tags.ts:29-31; src/lib/review.ts:953-955; src/styles/sidebar.css:230-231; src/styles/tokens.css:114-117
  Fix: Rewrite each in the same commit as the corresponding fix above (marker/queue with finding 2, btn-primary with finding 5, room badge/finder-tags with the DESIGN.md rewrite, review.ts with finding 1, sidebar/tokens with finding 7).

### Findings

- **[medium] One green answers to four names, and both tokens.css and DESIGN.md state the usage ranking backwards**
  #6CFF8D is --ella-green (tokens.css:10, 18 css + 2 tsx refs), --success (:64, 9 refs), --color-accent-green (:153, 3 css + 1 tsx refs), plus --stroke-green rgba(108,255,141,0.6) (:53, 1 ref). tokens.css:113-116 and docs/DESIGN.md:96 both claim '--color-accent-green is used more than either of its two older synonyms'; measured today it is the LEAST used of the three. CLAUDE.md:282-283 says 'the two green tokens'; there are four, and green-contract.test.ts:70 polices four. The same rule pair reaches the colour through two names four lines apart: shell.css:828 `.cp-drop-card.media { border-color: var(--stroke-green) }` then :829 `.cp-drop-card.media .cp-drop-icon { color: var(--success) }`. settings.css:1074 uses --success for `.cp-spike-row.pass` while settings.css:1202,1247 use --color-accent-green for `.cp-aiapi-msg.ok` in the same file for the same 'passed/ok' role.
  Evidence: src/styles/tokens.css:10,53,64,113-116,153; docs/DESIGN.md:96; CLAUDE.md:282-283; src/styles/shell.css:828-829; src/styles/settings.css:1074,1202,1247; src/lib/green-contract.test.ts:70
  Fix: Keep --success as the single semantic green (it is the name the rule is about) and --ella-green only for the brand mark (loader, welcome, nav edge, gen sparkle). Delete --color-accent-green (repoint review.css:309, settings.css:1202,1247 and the one TSX ref) and --stroke-green (shell.css:828 becomes color-mix(in srgb, var(--success) 60%, transparent)). Then correct the three prose sites: tokens.css:113-116, DESIGN.md:96, CLAUDE.md:282-283 ('two green tokens' -> name them). Narrow green-contract's GREEN regex to the two survivors.
- **[medium] Violet has a pure duplicate token and a hand-retyped rgb triplet that nothing pins**
  --novella-violet #6D52ED (tokens.css:12, 12 refs) is duplicated exactly by --color-accent-purple (tokens.css:154, 2 refs, both shell.css:830-831) and aliased by --marker (base.css:88) and --sel-fill (tokens.css:141). base.css:89 then hand-types `--marker-rgb: 109, 82, 237` for one use (transport.css:315 `rgba(var(--marker-rgb), 0.32)`). green-contract.test.ts:174-175 pins --accent-rgb to --accent's value precisely because 'tints and solids disagree' otherwise, but nothing pins --marker-rgb to --novella-violet; retune the violet and the timeline range fill silently keeps the old hue.
  Evidence: src/styles/tokens.css:12,141,154; src/styles/base.css:88-89; src/styles/shell.css:830-831; src/styles/transport.css:315; src/lib/green-contract.test.ts:174-175
  Fix: Delete --color-accent-purple (shell.css:830-831 -> var(--novella-violet)). Replace transport.css:315 with `color-mix(in srgb, var(--marker) 32%, transparent)` and delete --marker-rgb from base.css, which removes the retyped triplet rather than guarding it.
- **[medium] Three neutral aliases sit 4 units from a ladder rung; two were flagged by the prior audit and are still there**
  Token-vs-token distance scan: --stroke-1 #141419 vs --bg-2 #141415 (delta 4; 1 ref, settings.css:822 `border: 1px solid var(--stroke-1)`, a border role that tokens.css:55-62 says belongs to --line-*); --color-text-secondary #8A8A8E vs --fg-4 #86868F (delta 4; 1 ref, transcript.css:339); --color-surface-elevated #1A1A1D vs --bg-3 #1E1D20 (delta 4; 6 refs across ai.css:267, review.css:129,232, transcript.css:1508,1520,1565). DESIGN.md:75-78 says 'pick by depth, not by eye' and describes exactly this failure ('seven near-duplicate greys existed'). docs/DESIGN-AUDIT.md:90 asked for the first two to be collapsed; that document is linked from nowhere (grep of docs/, README, CLAUDE.md, CONTRIBUTING.md finds no reference) so the request was never seen.
  Evidence: src/styles/tokens.css:16-17,52,151-152; src/styles/settings.css:822; src/styles/transcript.css:339; docs/DESIGN.md:75-78; docs/DESIGN-AUDIT.md:90
  Fix: settings.css:822 -> var(--line-1); delete --stroke-1. transcript.css:339 -> var(--fg-4); delete --color-text-secondary. For --color-surface-elevated decide once whether floating surfaces are --bg-3 or a distinct step: if distinct, rename it into the ladder (e.g. --bg-3 stays, and the 6 refs move to a documented --bg-float) so it is visibly a rung; if not, repoint to --bg-3. Either delete docs/DESIGN-AUDIT.md or add a first-line 'superseded, kept for history' note and link it from DESIGN.md.
- **[medium] docs/DESIGN.md's Colour and Motion sections describe a token system that no longer exists**
  DESIGN.md:66-68: '`--accent` (`--ella-green`) means one of exactly two things: this is the primary action, or this is a notification' - base.css:81 makes --accent = --fg-1 and CLAUDE.md:282 says the opposite in the same words. DESIGN.md:88-91: 'A translucent brand colour is color-mix(in srgb, var(--accent) 20%, transparent)' - that now yields a white tint, so the recipe teaches the wrong thing. DESIGN.md:96: the inverted usage claim (see green finding). DESIGN.md:189: '`--ease-linear` for progress' - no such token; grep of src/, docs/, scripts/ finds it only on that line, while 6 bare `linear` literals (ai.css:76, coreview.css:389, library.css:138, monitor.css:1161, settings.css:415, transport.css:214) are exactly its would-be uses. DESIGN.md:235: 'node scripts/design-audit.mjs ... should print 0' - it prints 4 OUTLIERS. DESIGN.md:6: 'Tokens live in src/styles/tokens.css' while seven live in base.css. DESIGN.md:238-247 'What checks this' omits token-fallback-contract and green-contract.
  Evidence: docs/DESIGN.md:6,66-68,88-91,96,189,235,238-247; src/styles/base.css:58-82; CLAUDE.md:282; node scripts/design-audit.mjs output (4 OUTLIERS)
  Fix: Rewrite DESIGN.md Colour to match base.css:58-82 and green-contract: '--accent is the neutral emphasis (= --fg-1); green is --success for outcomes/live and --ella-green for the brand mark only'; change the tint recipe to `color-mix(in srgb, var(--success) N%, transparent)`. Either mint --ease-linear: linear and repoint the 6 literals, or delete the sentence. Fix the count claim at :96, state that base.css holds the accent/marker/focus tokens at :6, add the two missing tests to the table, and change :235 to the actual sanctioned count or fix the script (next finding). Add a docs-contract check that every `--x` named in DESIGN.md is defined in tokens.css or base.css.
- **[medium] focus-contract polices the retired meaning of --accent and misses two of the four live greens**
  src/lib/focus-contract.test.ts:17-18 GREEN = /--accent\b|--accent-rgb|--ella-green|--color-accent-green|108,\s*255,\s*141|#6cff8d|#88f362/i. --accent is now var(--fg-1), the exact 'brighten toward white' value the rule wants, so a compliant `:focus { border-color: var(--accent) }` fails while a non-compliant `:focus { outline-color: var(--success) }` or var(--stroke-green) passes. #88f362 appears nowhere in src/. docs/DESIGN-AUDIT.md:149-152 recorded the same gap.
  Evidence: src/lib/focus-contract.test.ts:17-18; src/styles/base.css:81; src/lib/green-contract.test.ts:70; docs/DESIGN-AUDIT.md:149-152
  Fix: Set focus-contract's regex to the same green token list green-contract uses (import or duplicate the four names, shrinking as tokens are deleted), drop --accent/--accent-rgb and #88f362, and add a canary asserting the regex matches at least one token name that tokens.css actually defines.
- **[medium] Seven tokens defined in base.css are outside all three tokens.css-only guards**
  --accent, --accent-rgb, --marker, --marker-rgb, --mark-wing-l, --mark-wing-r and --focus-ring are declared in base.css:57-119. design-tokens-contract.test.ts:135 reads only tokens.css for 'no unreferenced token', token-usage-contract.test.ts:33-41 builds its byValue map from tokens.css only, and token-fallback-contract.test.ts:31-34 builds `defined` from tokens.css only. So --marker-rgb (1 ref) can go dead unnoticed, a literal `rgba(255,255,255,0.45)` beside --focus-ring is never reported, and `var(--marker, #6d52ed)` would survive the fallback rule. --focus-ring is the app's most important single colour rule and its value is the least guarded.
  Evidence: src/styles/base.css:57-119; src/lib/design-tokens-contract.test.ts:135; src/lib/token-usage-contract.test.ts:33-41; src/lib/token-fallback-contract.test.ts:31-34
  Fix: Either move the seven declarations into tokens.css (they are tokens; base.css:58-119 is prose about them, which can move too), or have the three tests read a shared TOKEN_FILES = [tokens.css, base.css] the way scripts/design-audit.mjs:18 already does. The move is the smaller diff and also makes DESIGN.md:6 true.
- **[medium] Transition durations and easings have no contract, and the audit script undercounts them**
  tokens.css:304-318 defines five --dur-* speeds 'replacing 22 distinct transition durations', but design-tokens-contract has rules for font-size, weight, leading, tracking, radius and z-index and none for transitions. Comment-stripped scan: coreview.css:585 `transition: transform 120ms ease` (120ms IS --dur-fast); transport.css:538 `box-shadow 180ms ease-out` (180ms IS --dur-base); transport.css:537 `height 220ms`, transport.css:550-551 `190ms` x2 (off-scale, no rung within 10ms). Easing: the bare keyword `ease` appears 21 times and `linear` 6 times as literals though --ease-out/--ease-in-out/--ease-spring exist and DESIGN.md:189 promises --ease-linear. scripts/design-audit.mjs reports 1 transition outlier; it misses the four in transport.css because they sit on continuation lines without `transition:` on the same line.
  Evidence: src/styles/tokens.css:304-319; src/styles/coreview.css:585; src/styles/transport.css:537-538,550-551; docs/DESIGN.md:184-189; scripts/design-audit.mjs output '1 transition'
  Fix: Repoint coreview.css:585 to var(--dur-fast) var(--ease-out) and transport.css:538 to var(--dur-base); decide 190/220ms (round to --dur-base or --dur-slow, or name a rung). Add a `transition timings come from tokens` block to design-tokens-contract mirroring the line-height rule: brace-matched rule bodies, any `\d+m?s` or bare easing keyword inside a transition value that is not inside an @keyframes or the sanctioned ambient list fails. Fix design-audit.mjs to scan whole rule bodies rather than lines.
- **[medium] Border alphas within 0.02-0.04 of the line tokens, in exactly the roles those tokens name**
  tokens.css:55-62 says borders, dividers and hairline rings come from --line-1 (0.10) / --line-2 (0.16) / --line-3 (0.28). Border-value scan finds: room.css:412 `border-left: 1px solid rgba(255,255,255,0.12)` (a divider, delta 0.02 from --line-1); transcript.css:1687 and :1783 `border: 1px solid rgba(255,255,255,0.22)` on two swatch circles (same off-token literal twice, between line-2 and line-3); monitor.css:457 `.cp-prep-cancel:hover { border-color: rgba(255,255,255,0.24) }` (a hover border, the exact role of --line-3, delta 0.04). token-usage-contract matches hex only, so rgba literals are never compared to anything.
  Evidence: src/styles/tokens.css:55-62; src/styles/room.css:412; src/styles/transcript.css:1687,1783; src/styles/monitor.css:457
  Fix: room.css:412 -> var(--line-1); monitor.css:457 -> var(--line-3); transcript.css:1687,1783 -> var(--line-3) or, if the swatch ring genuinely needs 0.22, name it once. Extend token-usage-contract to parse `rgba(255,255,255,a)` on border/outline/box-shadow-inset declarations and report any a within 0.03 of a --line-* alpha.
- **[low] --font-ui and --font-sans are two names for one stack with no stated difference anywhere**
  tokens.css:270-272 defines --font-sans (37 refs), --font-ui (311 refs) and --font-mono (54 refs) with identical stacks. --font-mono's meaning ('digits matter here') is documented in tokens.css:200-204, DESIGN.md:56-60 and enforced by design-tokens-contract. --font-ui has no prose at all: grep of src/styles, docs/ and CLAUDE.md finds only its definition line. design-tokens-contract.test.ts:88-97 pins the three equal but does not say why there are two non-mono names. base.css:20 sets the body to --font-sans while 89% of component rules ask for --font-ui.
  Evidence: src/styles/tokens.css:270-272; src/styles/base.css:20; src/lib/design-tokens-contract.test.ts:88-97
  Fix: Collapse --font-sans into --font-ui (37 mechanical edits, base.css:20 included) and delete --font-sans, or write the one-line reason for the pair above tokens.css:270 and update the contract's comment to match.
- **[low] Stale prose still describes --accent as green at four sites**
  sidebar.css:446 '/* Green-tinted track + outer accent halo when ON */' above four var(--accent) declarations (:448-453) that now tint white. room.css:948 'accent green #6CFF8D reads 3.69:1' in a contrast rationale. src/lib/finder-tags.ts:29-30 'sits near this app's accent (#6CFF8D), which everywhere else means selected' - the accent is #F5F5F5 and green no longer means selected. docs/RECORDING-PLAN.md:246 cites `base.css:95` for --focus-ring, which is at base.css:118. These are what a reader hits first when asking what --accent means.
  Evidence: src/styles/sidebar.css:446-453; src/styles/room.css:944-949; src/lib/finder-tags.ts:29-30; docs/RECORDING-PLAN.md:246; src/styles/base.css:118
  Fix: Reword sidebar.css:446 to 'Brightened track + outer halo'; room.css:948 to 'the success green #6CFF8D'; finder-tags.ts:29-30 to 'near --success (#6CFF8D), which means an outcome here'; RECORDING-PLAN.md:246 to drop the line number.
- **[low] scripts/design-audit.mjs counts comment text as colour outliers and misses continuation-line durations**
  Running it reports 3 colour outliers: buttons.css:680 #A2A0AA, nav.css:88 #F5F5F5, room.css:948 #6CFF8D. All three are inside /* */ comments (the .cp-select chevron note, the nav-edge rationale, the plate-alpha rationale). The vitest contracts strip comments first; the script does not, so DESIGN.md:235's 'should print 0' cannot be true even when the stylesheets are clean. It also reports 1 transition outlier where the comment-stripped scan finds 5 (see the transitions finding).
  Evidence: scripts/design-audit.mjs:1-40 and run output; src/styles/buttons.css:676-681; src/styles/nav.css:84-90; src/styles/room.css:944-949; docs/DESIGN.md:235
  Fix: Strip comments with a newline-preserving replace before scanning, and scan brace-matched rule bodies for transition values. Then the 0 claim in DESIGN.md:235 holds once coreview.css:585 and the transport.css literals are fixed.
- **[low] Duplicated one-off literals typed twice across files, in CSS and in TS**
  CSS: #1a1410 at review.css:344 and transport.css:539 (ink on a light pin); #B084FF at logs.css:136 (`.tag.muxer`) and settings.css:331 (`.badge.recommended`) with its rgba(176,132,255,0.12) tint at :332. TS: "#AAAD98" (unassigned-speaker grey) at src/components/transcript/helpers.tsx:125 AND src/lib/cast.ts:204, where cast.ts revalidates a hex against a literal it does not import; src/lib/speech-kind.ts:53 NON_SPEECH_COLOR "#8E8E96" is 8 units from --color-text-secondary and --fg-4. DESIGN.md:116-119 says naming a colour used twice is a judgement call, so this is a pointer, not a violation.
  Evidence: src/styles/review.css:344; src/styles/transport.css:539; src/styles/logs.css:136; src/styles/settings.css:331-332; src/components/transcript/helpers.tsx:125; src/lib/cast.ts:204; src/lib/speech-kind.ts:53
  Fix: Export UNASSIGNED_SOLID from helpers.tsx and import it in cast.ts (a duplicated-tables-contract row can pin the two equal). Leave the CSS pairs unless a third use appears; if #B084FF is the 'muxer/recommended' lavender, it wants a name before the fourth copy.
- **[low] Two contracts report wrong line numbers because comment stripping collapses newlines**
  token-usage-contract.test.ts:60 and token-fallback-contract.test.ts:41 do `.replace(/\/\*[\s\S]*?\*\//g, " ")` then split on newline, so every multi-line comment above a hit shifts the reported line. The failure message tells a contributor to look at a line that holds something else; in library.css (2000+ lines, long comment blocks) the offset is dozens of lines.
  Evidence: src/lib/token-usage-contract.test.ts:58-62,88; src/lib/token-fallback-contract.test.ts:41
  Fix: Replace the comment with same-length whitespace preserving newlines: `.replace(/\/\*[\s\S]*?\*\//g, (m) => m.replace(/[^\n]/g, " "))`, the same technique reduced-motion-contract already needs.

### Findings

- **[high] Uppercase section-heading ("eyebrow") role has ~10 treatments, with collisions on the same surface**
  There are 63 text-transform: uppercase rules; the group/section heading among them is written with every combination of size (xs/sm/base/md), weight (semibold/bold/black), tracking (caps/caps-loose/caps-wide) and colour (fg-3/fg-4/fg-5). The visible collisions are within ONE surface. Drawer tabs: Transcript uses sm/caps-loose/bold/fg-4 (.cp-tx-history-head, .cp-tx-insights-head), Review uses the same but fg-3 (.cp-review-carried-head), AI uses base/caps/bold/fg-4 (.cp-ai-chapters-title). Settings: .cp-shortcut-cat-title (settings.css:437) and .cp-keybind-group-title (settings.css:904) are byte-identical apart from margin and head the same `group` strings (SettingsModal.tsx:2001, KeybindingEditor.tsx:54 mounted at SettingsModal.tsx:1979), while .cp-mediainfo-section h3 (settings.css:608) two tabs over is sm/caps-loose/bold. The same group names render a third and fourth way in the shortcut sheet (.cp-shortcuts-group h3 palette.css:250, xs/caps-wide/bold/fg-5) and the command palette (.cp-palette-group-label palette.css:105, xs/caps-wide/bold/fg-4). Sidebar: .cp-section-head .label (sidebar.css:54, fg-3) and .cp-section-label (sidebar.css:73, fg-4) are identical except colour and sit stacked in one column ("Source" at Sidebar.tsx:371 above the three CollapsibleSection heads at Sidebar.tsx:410/626/876).
  Evidence: src/styles/transcript.css:79,186; src/styles/review.css:1213; src/styles/ai.css:356; src/styles/settings.css:437,904,608,384; src/styles/palette.css:105,250; src/styles/sidebar.css:54,73; src/styles/shell.css:200; src/styles/coreview.css:510; src/styles/room.css:60,240; full list from the uppercase scan: 63 rules, 4 sizes x 3 weights x 3 tracking x 3 colours
  Fix: Define ONE eyebrow class in base.css next to .cp-keycap (suggest the plurality: font-size var(--text-sm); font-weight var(--weight-bold); letter-spacing var(--track-caps-loose); text-transform uppercase; color var(--fg-4)) and have the drawer heads, both Settings group titles, .cp-section-head .label/.cp-section-label, .cp-mediainfo-section h3 and .cp-shortcuts-group h3 compose it, deleting the per-file copies. Then add a shrink-only contract: every rule with text-transform: uppercase and a selector matching /head|title|label|group/ must declare exactly that tuple, allowlisting the genuine badges/chips (.cp-status-chip, .cp-lib-card-badge, .cp-review-badge, .cp-tab-soon).
- **[medium] The reader picker's selected row keeps its meta line on a colour chosen for bg-1**
  .cp-reader-row.selected paints bg-4 (shell.css:323) and .cp-reader-row-meta stays fg-4 (shell.css:351) with no selected override (the only .cp-reader-row-meta rules are :351 and :1107): fg-4 on bg-4 measures 4.12:1 on a 10px duration/date line. The library list row solved exactly this the day selection went purple (library.css:1348-1352 overrides kind/date/size/custom to --sel-ink, with a comment saying why); the reader picker did not get the same pass. The active row (bg-3, shell.css:315) sits at 4.65:1, which passes only because bg-3 is one step lighter.
  Evidence: src/styles/shell.css:315-356; src/styles/library.css:1344-1352; contrast: fg-4 #86868F on bg-4 #28272A = 4.12:1, fg-3 on bg-4 = 5.76:1
  Fix: Add .cp-reader-row.selected .cp-reader-row-meta { color: var(--fg-3); } directly under shell.css:323 with the same one-line comment the library row carries. Longer term, extend deep-state-contrast to select a reader row (Meta+5, click a row with shift) so the state is measured.
- **[medium] Permanent opacity fades on text-bearing containers push copy to 2-3.7:1, and both sweeps skip them by design**
  contrast.spec.ts:98 and deep-state-contrast.spec.ts:89 both `continue` on opacity < 0.99 as "faded-out states are transient". These are not transient: .cp-phase-track .step (transcript.css:1081) opacity .45 on fg-4, so the pending phase names read at about 2:1; .cp-review-comment.resolved (review.css:330) opacity .5, so its meta line (review.css:375, fg-4) lands near 2.4:1 and the body near 4.7; .cp-perms-row.granted (welcome.css:199) .62, so the description span (welcome.css:217, fg-4) is about 3:1; .cp-reader-group-count (shell.css:211) .75 on fg-4, about 3.7:1; .cp-mediainfo-info (settings.css:651) .45. The grey ladder already has a "receded" step (fg-4 to fg-5) that the tokens comment says is for exactly duplicated/decorative text; stacking opacity on top of a mid-grey is a second, unmeasured dimming axis.
  Evidence: e2e/contrast.spec.ts:98; e2e/deep-state-contrast.spec.ts:89; src/styles/transcript.css:1081-1090; src/styles/review.css:330,375; src/styles/welcome.css:199,217; src/styles/shell.css:207-213; src/styles/settings.css:651
  Fix: Express recession as a colour step, not container opacity: .cp-phase-track .step { color: var(--fg-5) } with .done/.active as now; .cp-review-comment.resolved keeps opacity for its avatar/chips but sets its body to fg-3 and meta to fg-4 explicitly; .cp-perms-row.granted fades the icon only. In both sweeps, composite opacity into the foreground alpha (fg[3] *= opacity) instead of skipping, and exempt only elements with a running transition.
- **[medium] Error text is painted in four different reds although --danger-text exists for exactly this**
  tokens.css:66-71 defines --danger-text (#FF8A8A) because --danger (#E82626) "at full saturation is heavy for a glyph and fails against dark greys"; --danger on bg-1 is 4.35:1, on bg-2 4.15, on bg-3 3.78. Twenty-two rules still write color: var(--danger) on text or glyphs, including .cp-review-drawhint.error (review.css:847) directly under .cp-review-drawhint.recording (:841) which correctly uses --danger-text, .cp-analysis-err (shell.css:473), .cp-rowmenu-err (:920), .cp-tx-tc-err (transcript.css:630), .cp-aiapi-msg.err (settings.css:1248), the spike verdict (:1075), the restricted AV permission (:1131), ai.css:81, room.css:461,469, library.css:2385,2471, monitor.css:1122, buttons.css:590,619. Two rules use a fourth red, --color-destructive #FF5C5C (coreview.css:327, settings.css:1037), and two a literal #ff9a9a (sidebar.css:216, queue-drawer.css:394) that token-usage-contract cannot flag because no token holds that value.
  Evidence: src/styles/tokens.css:66-71; grep 'color: var(--danger)' across src/styles = 22 hits; --color-destructive text uses: coreview.css:327, settings.css:1037; literal reds: sidebar.css:216, queue-drawer.css:394
  Fix: Mechanical: color: var(--danger) -> var(--danger-text) on every text/icon rule (leave background/border uses), #ff9a9a and --color-destructive -> --danger-text, delete --color-destructive (the unreferenced-token test will hold it dead). Add one line to token-usage-contract: a `color:` declaration may not name --danger or --danger-2; those are fill colours.
- **[medium] Settings explanatory copy has no single treatment**
  Inside one modal the paragraph that explains a control lands on: base/fg-3/leading-snug (.cp-pane-note settings.css:244, .cp-keybind-note :955, .cp-spike-note :1251 at leading-normal); base/fg-4 (.cp-ytauth-note :763, .cp-model-blurb :268, .cp-aiapi-hints :1223); md/fg-3 (.cp-update-note :1145, .cp-pane-sub :172); sm/fg-4 (.cp-cache-ret-note :1026); and six inline <p style> at 12px/fg-3/line-height 1.6 (SettingsModal.tsx:1699,1755,1785,1830,2103, 1546). The row description already has a canonical form (.cp-pane-row .k .desc, settings.css:215, base/fg-4/leading-snug). The same drift shows app-wide in the -note suffix: fg-3 in five rules, fg-4 in six, fg-5 in three (perms-note, tx-improve-note, shortcuts-note), and in -sub: fg-3 in six, fg-4 in seven.
  Evidence: src/styles/settings.css:172,215,244,268,763,955,1026,1145,1223,1251,1302; src/components/SettingsModal.tsx:1546,1699,1755,1785,1830,2103; secondary-line scan: 14 -note rules on three colour rungs, 13 -sub rules on two
  Fix: In Settings, make .cp-pane-note (base, fg-3, leading-snug) the one explanatory paragraph and .cp-pane-row .k .desc the one row description; retarget .cp-keybind-note, .cp-spike-note, .cp-ytauth-note, .cp-update-note, .cp-cache-ret-note and the six inline paragraphs to them and delete the extras. App-wide, decide -sub = md/fg-4/leading-normal and -note = base/fg-4/leading-snug (the plurality in each), and add both to the eyebrow contract above as role rows.
- **[medium] The size-to-tracking rule tokens.css states for uppercase is followed by 23 of 60 rules**
  tokens.css:233-235 names the three caps rungs by size: --track-caps "uppercase labels", --track-caps-loose "uppercase at 10px", --track-caps-wide "uppercase at 9px and below", and DESIGN.md repeats it ("Uppercase needs more air the smaller it gets"). Measured: of 60 uppercase rules with both a size and a tracking token, 23 follow that mapping, 33 do not, and 4 put a body rung (--track-wide, 0.02em) on uppercase (.cp-person-share room.css:479, .cp-person-presenting :936, .cp-aiapi-label settings.css:1150, .cp-perms-opt welcome.css:218). At the most common eyebrow size, --text-sm (10px), the three rungs are used 10/10/8, so the identical 10px caps label is 0.04em in .cp-reader-group-label (shell.css:200), 0.06em in .cp-tx-history-head (transcript.css:79) and 0.10em in .cp-section-label (sidebar.css:73). The contract only checks that a --track-* token is used, so the tokens' own documentation is the part nobody enforces.
  Evidence: src/styles/tokens.css:229-236; docs/DESIGN.md Tracking paragraph; scan output: follow 23, deviate 33, body-rung-on-caps 4; sm+caps 10, sm+caps-loose 10, sm+caps-wide 8
  Fix: Decide which is true. If the comment is the rule, add a block to design-tokens-contract: for any rule with text-transform: uppercase, --text-2xs/--text-xs require --track-caps-wide, --text-sm requires --track-caps-loose, larger requires --track-caps, and --track-wide/--track-slight/--track-tight are forbidden; then fix the 37 sites. If the CSS is the rule (three flavours, chosen by eye), rewrite the three comments to say so and drop the size claims from DESIGN.md. Either way the four body-rung uppercase rules are wrong under both readings.
- **[medium] --color-text-secondary is a third secondary-text grey kept alive by one placeholder glyph**
  --color-text-secondary (#8A8A8E, tokens.css:127) is four units off --fg-4 (#86868F) and sits in the --color-* alias generation that tokens.css:113 says "stalled halfway". Its only reference in the whole tree is .cp-cmd-empty-key (transcript.css:337-340), which styles the em-dash placeholder for an unbound shortcut (SettingsModal.tsx:2020). That single use is what keeps it past the unreferenced-token test, so the palette carries two near-identical "secondary text" values and the next person picks whichever they see first.
  Evidence: src/styles/tokens.css:127; src/styles/transcript.css:337-340; src/components/SettingsModal.tsx:2020; contrast on bg-1: --color-text-secondary 5.61:1, --fg-4 5.34:1
  Fix: transcript.css:339 -> color: var(--fg-4); delete --color-text-secondary from tokens.css (and move .cp-cmd-empty-key to settings.css, where its only consumer lives). The unreferenced-token test then keeps the alias from returning.
- **[medium] The tabular-nums system rests on a font feature the shipped Nunito Sans does not have**
  tokens.css:283-286 says "Nunito Sans supports font-variant-numeric: tabular-nums so timecode columns still line up"; base.css:8-13 says the family is "proportional" and "we'd lose timecode column alignment without" the universal rule; DESIGN.md says numerals are tabular everywhere; design-tokens-contract's first block requires every --font-mono rule to also declare tabular-nums, citing digits that "shimmied". Read from the woff2 files main.tsx imports (fontsource latin 300/400/400-italic/600/700/800): no face carries a tnum feature in GSUB or GPOS at all, and no pnum/lnum/onum either. The columns line up because every digit in every face has an advance of exactly 600 units by design. So the declaration is a no-op on `*` and in all 105 per-rule copies, the shimmy the contract describes cannot have happened with this font (the `*` rule has been present since the initial commit 4a4af1e, and every `font:` shorthand in the tree is `font: inherit`, which inherits it), and _design/design-audit.md section 1d had already called the per-rule copies redundant before r162 added ~96 more plus a contract demanding them. Nothing is wrong on screen today; the risk is that the guard guards nothing, and a font swap to a face with proportional default figures would misalign every timecode with every test green.
  Evidence: fontTools over node_modules/@fontsource/nunito-sans/files/nunito-sans-latin-{300,400,600,700,800}-normal.woff2 and 400-italic: GSUB features [calt, ccmp, dnom, frac, liga, locl, numr], no GPOS tnum, digit advances all 600; src/styles/base.css:5-13; src/styles/tokens.css:283-286; src/lib/design-tokens-contract.test.ts first describe block; _design/design-audit.md:84-88; grep 'font:' in src/styles = 10 hits, all `font: inherit`
  Fix: Keep the `*` rule (harmless, and correct if the font ever changes). Retire the per-rule requirement from design-tokens-contract and replace it with the check that actually holds the columns: a vitest that opens each imported latin woff2 (fontTools is not available in Node; use `fontkit` or parse hmtx/cmap directly, both a few lines) and asserts either a tnum feature or equal advances for U+0030..U+0039 in every face. Rewrite the three comments to say digits are monowidth by the font's design, not by the property.
- **[low] Key/value label rows are uppercase in two places and sentence-case in two others**
  The definition-list key is caps in the sidebar (.cp-kv .k sidebar.css:179, sm/caps-loose/normal/fg-4) and the model info popover (.cp-model-info-popover dt settings.css:384, xs/caps-loose/semibold/fg-4), but sentence-case in Settings > About (.cp-about-row .k settings.css:549, base/fg-4) and Media Info (.cp-mediainfo-row .k settings.css:628, md/fg-4). Values differ too: fg-2 in sidebar and About, fg-1 for .v-primary in Media Info (:639).
  Evidence: src/styles/sidebar.css:175-192; src/styles/settings.css:384,545-550,622-645
  Fix: Pick the About/Media Info form (sentence-case key on fg-4, value on fg-2) for all four; it is the one that does not fight the eyebrow role, and .cp-kv .k then stops being one more uppercase label competing with the section heading above it.
- **[low] DESIGN.md documents --accent as the brand green three days after base.css made it neutral**
  docs/DESIGN.md:66 opens the Colour section with "--accent (--ella-green) means one of exactly two things: this is the primary action, or this is a notification." base.css:65-89 sets --accent: var(--fg-1) and explains at length that the accent is NEUTRAL, and green-contract enforces it. DESIGN.md was last touched in 3bb26be (2026-08-23); the accent change landed in 9079034 (2026-08-26). The page is the reference CLAUDE.md tells people to read before adding a style, so someone choosing color: var(--accent) for a label (as .cp-person-presenting room.css:936 and .cp-review-drawhint review.css:833 do) expects green and gets white. docs-contract does not read DESIGN.md.
  Evidence: docs/DESIGN.md:66; src/styles/base.css:65-89; git log: 3bb26be vs 9079034; src/lib/docs-contract.test.ts has no DESIGN.md reference
  Fix: Rewrite DESIGN.md's first Colour paragraph to match base.css (accent is the emphasis neutral; green is --success / --ella-green and means outcome or live only), and add a docs-contract assertion that the token DESIGN.md names as --accent's value matches the definition in base.css.

### Findings

- **[medium] The drop-target state is drawn five different ways for one meaning (and not at all in a sixth place)**
  Tree row (library.css:2492-2495): `color-mix(--accent 18%)` fill + inset 1px `--accent 55%` ring. Breadcrumb (:2500-2503): `--accent 20%` fill + the same ring. Folder tile (:2415-2418): `border-color: var(--fg-2); background: var(--bg-3)`. Collection shelf (:2423-2427): `outline: 1px solid var(--fg-2); outline-offset: 4px`. Reader project/group label (shell.css:1050-1054): `outline: 1px solid var(--fg-2); outline-offset: 2px`. List folder row: nothing (finding 5). Each comment says "same language as" a sibling while using a different drawing. A maintainer adding the next drop target has five precedents and no token.
  Evidence: grep of `\.dropping` across src/styles returns exactly those five rule sites; grep of `" dropping"` across src/components returns six emitters (CachedWebPane.tsx:371, LibraryFolderCard.tsx:62, LibraryFolderRow.tsx:56, LibraryTree.tsx:377, ReaderProjectHeader.tsx:69 and :94).
  Fix: Add `--drop-fill: color-mix(in srgb, var(--fg-1) 18%, transparent)` and `--drop-ring: inset 0 0 0 1px color-mix(in srgb, var(--fg-1) 55%, transparent)` to tokens.css, write the five (six) `.dropping` rules as `background: var(--drop-fill); box-shadow: var(--drop-ring)`, and let the emitter-vs-rule contract from finding 5 hold the set.
- **[medium] Row hover, focus-lift and "current row" fills are per-file numbers with no token; nine hover alphas and six current-row treatments**
  Row-shaped hover fills: 0.025 (settings.css:455, :922), 0.03 (transcript.css:1250, :1389, :1820), 0.04 (transcript.css:121, :1892, :1969), 0.05 (library.css:1036, :1331), 0.06 (library.css:2142; transcript.css:2265; review.css:1054), 0.07 (library.css:658), 0.08 (review.css:1167; library.css:373), `--bg-2` (shell.css:314), `--bg-3` (library.css:461; shell.css:874), `--bg-4` (library.css:2549; transcript.css:1746). Focus lift 0.075 on list rows (library.css:1332) vs 0.08 on tree rows (:1037). Single "current" row: reader-row.active bg-3 + line-2 (shell.css:315-319); tx-history-row.active `--accent 8%` + accent left bar (transcript.css:122-125); review-veritem.current 0.10 (review.css:1168); recents-row.active 0.06 (toolbar.css:123); palette-row.active 0.08 + inset line-3 (palette.css:123-131); model-row.selected accent 40% border + 5% fill + `--ring-selected` (settings.css:285-289). DESIGN.md:80-82 says fill alphas keep their own values, but that sentence is about not borrowing `--line-*` for fills; it does not settle nine different hover alphas on rows.
  Evidence: grep `:hover {[^}]*background` across the stylesheets, filtered to row/item/card/cue selectors, listed above with line numbers; the two most-trafficked lists (library.css:1036 and :1331) already agree on 0.05.
  Fix: Add `--row-hover: rgba(255,255,255,0.05)`, `--row-focus: rgba(255,255,255,0.08)` and `--row-current: rgba(255,255,255,0.08)` to tokens.css, adopt them in the row-shaped selectors listed (leave button/menu-item hovers alone), and change the literal regexes in list-zebra-contract.test.ts:50-52 to read the tokens; then a `var(--row-*)` presence check in that contract stops the tenth alpha.
- **[medium] Multi-select rows announce every selected row as the single `aria-current`, and the reader's selection has no state at all**
  LibraryListRow.tsx:88, WebListRows.tsx:91, FrameListRows.tsx:84 and LibraryCard.tsx:210 set `aria-current="true"` on each selected row (LibraryListRow.tsx:59-61 documents choosing it over a table role). aria-current names THE current item in a set; with a shift-range, three or ten rows carry it, which a screen reader relays as a wrong state. The tree uses `aria-selected` on `role="treeitem"` correctly (LibraryTree.tsx:369-371). The Transcripts picker's `.selected` (TranscriptReader.tsx:597) has no ARIA state; its aria-current at :613 tracks only the open transcript.
  Evidence: Measured on a selected Frames row: ariaCurrent "true", ariaSelected null. Four files set the attribute the same way; one sets nothing.
  Fix: The rows already sit in `role="list"` / `role="listitem"` containers: switch those containers to `role="listbox" aria-multiselectable="true"` with `role="option" aria-selected` on the row/card button (the roving tabindex is unchanged), and give `.cp-reader-row` the same. Add the check to e2e/accessible-names.spec.ts so a fifth list cannot pick a third convention.
- **[low] Dead and no-op rules in the list stylesheets**
  `.cp-web-card { position: relative }` (library.css:1989) and `.cp-web-card:hover .cp-web-forget` (:2009): `cp-web-card` is emitted by no component; its only occurrence in src/components is a comment at CachedWebPane.tsx:169 saying the card was replaced. `.cp-lib-tree-web .cp-lib-tree-name { color: inherit }` (library.css:1921): `.cp-lib-tree-name` (:1073-1079) sets no colour, so the override changes nothing. component-reachable-contract covers components; nothing covers a class a stylesheet targets that no component emits.
  Evidence: grep -rn "cp-web-card" src/components src/*.tsx returns only the CachedWebPane.tsx:169 comment; `.cp-lib-tree-name` declarations contain no `color`.
  Fix: Delete the three rules. If it is worth guarding, class-prefix-contract already tokenises every selector; a sibling assertion that each `cp-` class in a stylesheet appears in some .tsx source would have flagged these.

### Findings

- **[high] A keyboard-opened modal leaves popovers standing, and the ladder then paints the popover ABOVE the modal** *(partly done: Cmd-, now dispatches the dismiss; the 14 legacy hand-rolled dismissers still ignore it)*
  Two gaps compound. (1) DISMISS_POPOVERS is dispatched only for app.palette and app.shortcuts (use-keyboard-shortcuts.ts:133, 137); app.settings at :140 opens Settings without it. (2) Only the 8 useDismiss callers plus RecentSources honour the event; the 14 other legacy files (17 document-level listeners) ignore it, including LibraryCardMenu, FolderTagMenu, NotificationBell, ViewOptions, VolumeControl, SpeedControl, EmojiPicker, CueSelectionMenu. Then the rung order makes it visible: --z-modal is 100, the lowest rung, beneath --z-menu 200, --z-popover 400, --z-notify 500 and --z-overlay 700. Concretely: right-click a library card, press Cmd-K. The card menu is portaled to body at position:fixed z 700 (library.css:634-635, LibraryCardMenu.tsx:184) and does not listen for the event, so it stays painted OVER the command palette scrim at 600 until the first click. Or open the bell, press Cmd-,: the notifications popover (portaled, fixed, z 500, NotificationBell.tsx:121-131, monitor.css:993) paints over the Settings backdrop at z 100 (settings.css:28). The e2e popover-under-modal spec only checks the recents popover under the palette.
  Evidence: src/hooks/use-keyboard-shortcuts.ts:132-140 (dispatch on palette/shortcuts, none on settings); src/hooks/use-dismiss.ts:62 (only hook users listen); src/components/RecentSources.tsx:79 (the one manual subscriber); src/lib/dismiss-parity-contract.test.ts:46-62 (the 15 legacy files); src/styles/tokens.css:293-300 (modal 100 is the lowest rung); src/styles/library.css:634-635 (.cp-lib-menu at --z-overlay); src/styles/settings.css:28 (.cp-modal-backdrop at --z-modal); e2e/popover-under-modal.spec.ts:1-25 (covers recents only)
  Fix: Dispatch DISMISS_POPOVERS from every keyboard/menubar path that opens a modal (app.settings at :140; also the native-menu Settings binding), and make the event a document-level concern rather than a per-popover opt-in: have every dismisser answer it, either by migrating the 14 legacy files to useDismiss or by exporting a tiny useDismissAll(onClose) from use-dismiss.ts that the legacy effects call. Then extend e2e/popover-under-modal.spec.ts to open Settings with Cmd-, over the bell popover and assert the popover is gone. Do not solve it by raising --z-modal above the menus; the ladder order is deliberate (a menu launched FROM a modal must outrank it).
- **[medium] HistoryPopover is role="menu" but does not use useMenuKeys, so arrows/Home/End/type-ahead and focus-return are missing in this one menu**
  use-menu-keys.ts:3-40 says it exists so that every role="menu" in the app keeps the ARIA promise, and lists eleven that were wired. HistoryPopover.tsx:77 declares role="menu" with role="menuitem" rows, but the file imports only useDismiss (line 4); it is absent from the useMenuKeys caller list. Its rows carry tabIndex={0} (line 125), so every history entry is a Tab stop, arrows do nothing, and nothing hands focus back to the trigger on close (useDismiss does not restore focus; useMenuKeys does at lines 150-162). The e2e popover-focus spec measures three toolbar popovers only.
  Evidence: src/components/transcript/HistoryPopover.tsx:4, :59, :77, :115-134; src/hooks/use-menu-keys.ts:5-13, :150-162; useMenuKeys callers (12 files) exclude HistoryPopover; e2e/popover-focus.spec.ts:18-22
  Fix: Add useMenuKeys(popRef, true, onClose) to HistoryPopover (it is mounted only while open, same as FolderTagMenu.tsx:42), drop tabIndex={0} from the rows (the hook sets roving -1), and add "Transcript history" to the TRIGGERS list in e2e/popover-focus.spec.ts so focus-return is measured.
- **[medium] .cp-sharemenu is a copy of .cp-colmenu, and both are the fifth recipe for the same role="menu" surface**
  Container: .cp-sharemenu (room.css:1049-1057, commit 47528f2, 2026-09-01) and .cp-colmenu (library.css:2531-2542, commit a006533) share position:fixed, z-index var(--z-menu), padding var(--s-1), background var(--bg-2), border 1px solid var(--line-2), border-radius var(--r-card), box-shadow 0 10px 28px rgba(0,0,0,0.5); they differ only in min-width (260 vs 168) and colmenu's font lines. Items: identical hover (var(--bg-4)/var(--fg-1)) and focus-visible rules, but min-height 30 vs 26, padding s-1/s-2 vs 3px/s-2, gap s-3 vs s-2, align baseline vs center. Both components use useDismiss + useMenuKeys. Zoom out and the eight role="menu" containers use FIVE surface recipes: bg-2/r-card/0 10px 28px (colmenu, sharemenu), bg-2/r-md/--shadow-card (lib-menu, cuemenu-flyout), --color-surface-elevated/r-md/--shadow-card (ai-export-menu, review-export-menu - byte-identical to each other bar min-width 184/224), bg-1/r-md/0 12px 36px (rowmenu), bg-1/r-sm/--shadow-card/padding 4px 0 (tx-dl-menu). Items across 10 menu-item classes use 6 hover colours (rgba .07 x3, .06, .04 x2, .02, --bg-4 x2, --bg-3), 7 paddings (8/12, 3/8, 4/8, 7/10 x3, 6/12, 7/9, 10/12) and two text treatments (lib-menu-item is fg-1 semibold; every other item is fg-2 normal). Item heights land at 26, 26.4, 28.4, 30 and ~31px. Nothing enforces any of it: token-usage-contract checks hex only, design-tokens-contract checks radius rungs only.
  Evidence: src/styles/room.css:1049-1080; src/styles/library.css:2531-2563; src/styles/library.css:634-664; src/styles/ai.css:258-283; src/styles/review.css:124-150; src/styles/shell.css:851-874; src/styles/transcript.css:633-660; role="menu" sites: AiSummary.tsx:754, FolderTagMenu.tsx:91, LibraryCardMenu.tsx:181, ListColumnHeaders.tsx:315, ProjectMenu.tsx:106, ReaderRowMenu.tsx:113, SourceShareMenu.tsx:45, SpeedControl.tsx:71, TranscriptViewer.tsx:1837/1919, ViewOptions.tsx:133, HistoryPopover.tsx:77
  Fix: Yes: one shared menu class. Add src/styles/menu.css (imported from index.css like the other section files) with .cp-menu, .cp-menu-item, .cp-menu-sep, .cp-menu-check using the colmenu recipe (bg-2 / line-2 / --r-card / --s-1 padding; item min-height 28px, padding 4px var(--s-2), --r-sm, fg-2, hover var(--bg-4) + fg-1, the shared focus-visible ring) and a --menu-min-width custom property for the one thing that legitimately varies. Migrate in this order, each a diff that deletes a block: colmenu + sharemenu (already the same), ai-export + review-export (already the same), then lib-menu, rowmenu, tx-dl-menu, cuemenu-flyout. Guard it per element the way scrim-layer-contract does: every JSX element with role="menu" must carry cp-menu, and no stylesheet other than menu.css may declare background or box-shadow on a selector ending in -menu.
- **[medium] Fourteen distinct box-shadows on floating surfaces from a two-token shadow set**
  tokens.css:76-81 defines --shadow-soft and --shadow-card and says shadows are for floating layers, tight and dark. Floating surfaces use --shadow-card in 14 places and then twelve hand-written shadows besides: 0 10px 28px .5 (colmenu, sharemenu, tx-improve-pop), 0 8px 12px .55 (notif, recents), 0 8px 12px .5 + 0 0 0 .5px line-1 (view-popover), 0 6px 12px .55 (volume, speed), 0 6px 12px .45 (toast), 0 12px 36px .5 (rowmenu), 0 8px 22px .5 (react-pop), 0 4px 12px .5 (web-collect-pop, annot-color-popover), 0 18px 50px .6 (share-dialog), 0 24px 60px .55 (rowmenu-dialog), 0 24px 64px .55 + ring (spk-modal), 0 0 0 .5px + 0 30px 90px .6 (cp-modal), inset + card + ring (palette, shortcuts). No contract mentions box-shadow (grep over src/lib/*-contract.test.ts finds none), and token-usage-contract only matches hex, so rgba shadows are invisible to it.
  Evidence: src/styles/tokens.css:76-81; monitor.css:565, :765, :845, :887, :992; library.css:2539, :2122; room.css:1056, :720; shell.css:858, :882; review.css:981; transcript.css:972, :1148; settings.css:46; toolbar.css:104; palette.css:22-35
  Fix: Add two rungs to tokens.css beside --shadow-card: --shadow-menu (0 10px 28px rgba(0,0,0,.5), the value the two newest menus and tx-improve-pop chose) and --shadow-dialog (0 24px 64px rgba(0,0,0,.55)), then replace the twelve literals. Extend token-usage-contract with a box-shadow block: a literal box-shadow whose value equals a --shadow-* token is an offender, using the same exact-duplicate rule the hex check already applies.
- **[medium] Seven recipes for "dim the app behind a dialog"**
  .cp-modal-backdrop/.cp-modal-scrim: rgba(0,0,0,.55) + blur 6px + fade dur-base. .cp-palette-scrim and .cp-lib-thumb-scrim: rgba(5,5,8,.55) + blur 10px + fade dur-fast. .cp-spk-backdrop: rgba(0,0,0,.5), no blur, fade dur-fast. .cp-share-dialog-backdrop: rgba(0,0,0,.55), no blur, no animation. .cp-rowmenu-scrim.modal: color-mix(bg-0 55%), no blur, no animation. .cp-ql-scrim: rgba(0,0,0,.72) composed over modal-scrim's blur. .cp-framepv-scrim: bg-0 88% (this one is documented as deliberate at library.css:2244-2246 and is not a finding). The dialogs behind them split the same way: bg-1 x4 (share-dialog, rowmenu-dialog, rename, ql) vs bg-2 x3 (modal, spk-modal, lib-thumb); share-dialog is the only dialog bordered with --line-1 instead of --line-2; entrance animations are cp-modal-in at dur-slow (modal), cp-modal-in at dur-base (spk-modal), cp-palette-in at dur-base (lib-thumb), cp-palette-in at a literal 180ms (palette, shortcuts), and none (share, rowmenu-dialog, rename, ql). scrim-layer-contract checks only that a scrim is a fixed layer, not what it looks like.
  Evidence: settings.css:18-30; palette.css:9-20; library.css:735-746, :1803, :2236-2247; transcript.css:1129-1137; room.css:701-721; shell.css:850, :875-882; animations at settings.css:52, transcript.css:1151, library.css:757, palette.css:35 and :218
  Fix: Make .cp-modal-backdrop the one scrim: give the four other scrim classes only their composition role (.cp-ql-scrim already does this correctly: className="cp-modal-scrim cp-ql-scrim", darkening only). Concretely, ShareDialog, SpeakerRosterModal, NewSpeakerSheet, ProjectMenu and ReaderRowMenu render className="cp-modal-backdrop <own-class>" and the own-class blocks lose position/inset/z-index/background/blur/animation. Pick bg-2 + --line-2 + one entrance (cp-modal-in at --dur-base) for the dialog box and apply it to the seven dialog classes; palette and shortcuts keep their r-xl treatment since DESIGN.md names them.
- **[medium] DESIGN.md's radius table describes 9 of 28 popovers and 2 of 9 dialogs**
  docs/DESIGN.md:142 says --r-card (10px) is for surfaces that float: popovers, cards, panels, toasts; :144 says --r-xl (16px) is for modals and the command palette. Measured: of 28 popovers/menus, 9 use --r-card (colmenu, sharemenu, tx-improve-pop, notif-popover, recents-pop, web-collect-pop, annot-color-popover, badgesheet, canvas-toast), 16 use --r-md (lib-menu, rowmenu, ai/review export, review-history, tx-tc-pop, tx-history, tx-insights, view/volume/speed popovers, react-pop, verpop, model-info-popover, cuemenu-flyout, tx-rename), 2 use --r-lg (emoji-pop, colorpick) and 1 uses --r-sm (tx-dl-menu). Of 9 dialogs, 7 use --r-lg (cp-modal, spk-modal, share-dialog, rowmenu-dialog, rename, ql, lib-thumb) and only palette/shortcuts use --r-xl. The tokens.css comment at 165-172 is accurate about what r-card was cut FROM (14 existing 10px declarations); DESIGN.md turned that into a rule for the category and the category does not follow it. design-tokens-contract checks that a radius is a rung, not which rung.
  Evidence: docs/DESIGN.md:134-153; src/styles/tokens.css:165-180; radius declarations at library.css:640, :2538, :1694, :1810, :755; room.css:1055, :719; monitor.css:563, :764, :844, :987, :1229, :886; review.css:130, :233, :731, :980, :1151; transcript.css:73, :180, :606, :640, :969, :1146, :2105, :2316; shell.css:857, :881; settings.css:45, :362; toolbar.css:103; palette.css:27, :213
  Fix: Decide once and make the doc and the code agree. Given 16 vs 9, the cheaper truth is: menus and small anchored popovers are --r-md, larger floating panels (notifications, recents, toast, sheets) are --r-card, dialogs are --r-lg, and only the palette family is --r-xl. Rewrite DESIGN.md:142-144 to say that, move the four outliers (tx-dl-menu r-sm, emoji-pop and colorpick r-lg, and either colmenu/sharemenu or the rest of the menus) onto the chosen rung, and add a per-category assertion to design-tokens-contract: a selector ending in -menu uses --r-md, a selector ending in -backdrop's sibling dialog uses --r-lg.
- **[medium] --color-surface-elevated is a fourth grey that only four floating surfaces use**
  tokens.css:151 defines --color-surface-elevated as #1A1A1D, which sits between --bg-2 (#141415) and --bg-3 (#1E1D20). Across all stylesheets it is used six times: as the background of .cp-ai-export-menu, .cp-review-export-menu, .cp-review-history-menu and .cp-colorpick, and as a focus-ring gap colour twice in transcript.css. So the AI Summary export menu is a different grey from the transcript Tools menu next to it, and the review export menu is a different grey from the review Versions popover in the same panel. Popover backgrounds overall: solid --bg-2 x11, --bg-1 x9, --color-surface-elevated x4, --bg-3 x1, --bg-2 at 96% x2, --bg-2 at 92% x1.
  Evidence: src/styles/tokens.css:151; ai.css:267; review.css:129, :232; transcript.css:1508, :1520, :1565
  Fix: Retire --color-surface-elevated: point the four backgrounds at --bg-2 (or --bg-3 if a lift over bg-2 is wanted) and the two ring gaps at --bg-2, delete the token, and let design-tokens-contract's unreferenced-token check hold the line. Then the popover background question is bg-1 vs bg-2, which the shared menu class settles.
- **[medium] Two full-viewport modal scrims sit at z-index 60, below the ladder and invisible to the contract**
  .cp-spk-backdrop (SpeakerRosterModal and NewSpeakerSheet) and .cp-share-dialog-backdrop (ShareDialog) are position:fixed; inset:0 aria-modal scrims at z-index: 60, while the equivalent .cp-modal-backdrop is on --z-modal (100). 60 is also the value of two persistent fixed/absolute panels: .cp-reader-stage-panel.floating (shell.css:508) and .cp-queue-drawer.open (shell.css:758). SpeakerRosterModal is rendered inline in TranscriptViewer (no createPortal), so its scrim ties the floating stage panel at 60 in the same root context and wins only because <TranscriptViewer> (App.tsx:4525) happens to follow <ReaderPlayerStage> (App.tsx:4505) in DOM order. design-tokens-contract's z-index check (LOCAL_CEILING 99) treats anything at or below 99 as local stacking and does not see these, and DESIGN.md:170 says values at or below 99 are local, which a full-viewport scrim by definition is not.
  Evidence: src/styles/transcript.css:1129-1132; src/styles/room.css:701-704; src/styles/shell.css:506-510, :756-760; src/components/transcript/SpeakerRosterModal.tsx (no createPortal); src/App.tsx:4505, :4525; src/lib/design-tokens-contract.test.ts:332-356; docs/DESIGN.md:170-172
  Fix: Set both scrims to z-index: var(--z-modal) (or compose .cp-modal-backdrop as in the scrim finding), and teach scrim-layer-contract one more property: a class that makes an element a fixed inset layer must also take its z-index from a --z-* rung. That closes the gap between the two contracts, since neither currently asks.
- **[medium] z-index: calc(var(--z-menu) + 10) is the between-rungs value DESIGN.md forbids, and the contract cannot see it**
  .cp-tx-tc-pop declares z-index: calc(var(--z-menu) + 10), i.e. 210, with a comment saying it must sit one above the download menu it can open beside. DESIGN.md:174-176 says: if you need a value between two rungs, you need a stacking context, not a bigger number. design-tokens-contract matches z-index literals with /z-index\s*:\s*(-?\d+)/ and counts var() uses separately, so a calc() passes both checks. Both popovers are absolute inside .cp-tx-tools (TranscriptViewer.tsx:1823), so the relationship is local and does not need an app rung at all.
  Evidence: src/styles/transcript.css:600-614; docs/DESIGN.md:174-176; src/lib/design-tokens-contract.test.ts:348-352, :364
  Fix: Give .cp-tx-dl-menu and .cp-tx-tc-pop small local integers (e.g. 20 and 21) since .cp-tx-tools is their shared parent, or make .cp-tx-tools position:relative; z-index: var(--z-menu) and let the two children use 1 and 2. Then add calc( to the contract's offender pattern so a computed rung fails like a literal one.
- **[low] Toast advertises a click it no longer has**
  .cp-canvas-toast still sets cursor: pointer on the whole toast (monitor.css:889), but CanvasToast.tsx:81-90 removed the whole-toast dismiss in favour of the x button (the comment says so). Hovering the toast shows a hand cursor and clicking does nothing; hover only pauses the countdown.
  Evidence: src/styles/monitor.css:876-891 (cursor: pointer at 889); src/components/CanvasToast.tsx:70-90
  Fix: Delete cursor: pointer from .cp-canvas-toast; the close button already has its own pointer cursor at monitor.css:948.
- **[low] Stale stacking comments still quote the pre-ladder numbers**
  Comments written for the old 4000..10002 scheme survived the r162 ladder: palette.css:12 "one above the notification popover (9999)", shell.css:790 "one above the command palette (10000)", library.css:635 "above the thumbnail modal's tier so a menu-launched flow reads on top" (the card menu closes on select at LibraryCardMenu.tsx:202, so it is never open while the picker is), monitor.css:981-986 "z-index 9999 sits above the queue drawer ... below the SettingsModal overlay which uses z 100" (the popover is now 500 and Settings is 100, the opposite of what the comment claims), tokens.css:291 "The five app-level layers" above a list of eight, and tokens.css:295 "nothing outranks first run" with five rungs above it. These are the exact each-author-picked-one-higher notes the ladder replaced, and they now tell a new reader the wrong order.
  Evidence: src/styles/palette.css:12; src/styles/shell.css:790; src/styles/library.css:635; src/styles/monitor.css:981-986; src/styles/tokens.css:291, :295
  Fix: Rewrite each to name the rung's meaning ("--z-notify: above panels, below the palette scrim"), fix "five" to "eight", and change --z-firstrun's note to "nothing INSIDE THE APP outranks first run; the five rungs above it are transient surfaces the welcome cannot open".
- **[low] Three var(--r-md, 8px) fallbacks re-type the token value, outside token-fallback-contract's reach**
  token-fallback-contract forbids a duplicate HEX fallback on a token tokens.css defines. .cp-react-pop, .cp-share-card and .cp-share-portion-surface carry var(--r-md, 8px): a px duplicate of the same kind, which the contract's hex regex does not match. If --r-md is ever retuned these three keep the old value silently.
  Evidence: src/styles/review.css:980; src/styles/room.css:786, :815
  Fix: Drop the three fallbacks to var(--r-md) and widen token-fallback-contract's value pattern from hex to any literal (px, rgba, hex).
- **[low] SourceShareMenu is the only fixed context menu not portaled to body, and it lives under a container-type ancestor**
  Every other position:fixed menu is portaled (LibraryCardMenu, FolderTagMenu, HistoryPopover, NotificationBell, RecentSources). .cp-sharemenu is position:fixed (room.css:1050) but SourceShareMenu.tsx renders inline, inside .cp-room-head, which sets container-type: inline-size (room.css:49). Older WebKit applied layout containment to query containers, which would make the header the containing block for the fixed menu and offset it by the header's position. A probe in the repo's Chromium (1228) places the fixed child at the viewport (OK); WebKit is not installed in the Playwright cache here, so the shipping engine could not be checked. Not asserting breakage; asserting that it is the one menu whose correctness depends on a containment detail the others avoided.
  Evidence: src/styles/room.css:42-58 (container-type: inline-size), :1049-1051; src/components/SourceShareMenu.tsx:42-49 (no createPortal); src/App.tsx:4695-4700
  Fix: Wrap the return in createPortal(..., document.body) like LibraryCardMenu.tsx:178, which also removes the menu from the header's flex/containment entirely. Confirm once in npm run tauri dev that the menu lands under the button.

### Findings

- **[high] Ten close (x) button families at six sizes, two radii and five glyph sizes; DESIGN.md assigns close buttons `--r-sm` and four of them use `--r-xs`**
  A user closing the Settings modal, a thumbnail picker, the detail pane, a drawer tab, a toast, a hint bar, a range chip and a link offer meets a different x every time: 28px with a border that appears on hover, 28px always bordered, 24px flat, 19px on a 6% fill, 20px flat, or an unsized text button. The glyph itself is drawn at --text-base, -lg, -xl, -2xl and -3xl. docs/DESIGN.md:140 says `--r-sm` is for close buttons; four use `--r-xs`.
  Evidence: src/styles/settings.css:90 `.cp-modal-close` 28px r-sm fs-xl border transparent; src/styles/library.css:773 `.cp-lib-thumb-close` 28px r-sm border line-2; src/styles/transcript.css:1178 `.cp-spk-x` 26px r-sm fs-3xl; src/styles/library.css:1426 `.cp-lib-detail-close` 24px r-xs fs-lg; src/styles/queue-drawer.css:255 `.cp-tab-close` 24px r-xs fs-3xl; src/styles/transcript.css:1011 `.cp-tx-hint-close` 20px r-xs fs-2xl; src/styles/monitor.css:934 `.cp-canvas-toast-close` 20px r-xs fs-2xl; src/styles/review.css:1197 `.cp-review-linkoffer-x` 20px r-sm fs-base; src/styles/review.css:674 `.cp-review-range-x` 19px r-sm fs-xl bg 6%; src/styles/library.css:1832 `.cp-ql-close` no size, padding 2px 6px, r-sm fs-lg. docs/DESIGN.md:140 `--r-sm | 6px | small tiles, chips, thumbs, close buttons`.
  Fix: One primitive in buttons.css: `.cp-close { width: 24px; height: 24px; padding: 0; border: none; border-radius: var(--r-sm); background: transparent; color: var(--fg-3); }` with `:hover { background: rgba(255,255,255,0.08); color: var(--fg-1); }` and a single SVG glyph (an `IconClose` at 12px) instead of a text x at five type rungs. Put it on all ten; the 19/20px ones in KNOWN_SMALL grow to 24 and leave that list (hit-target-contract requires it). Keep `.cp-modal-close` at 28 only if the modal header needs it, as a documented `.cp-close.lg`.
- **[medium] `.btn` has no size rungs, so eight heights are imposed by context selectors, and the smallest one is named `cp-tx-iconbtn` although it is a text button**
  buttons.css gave `<select>` an explicit three-rung ladder (30/26/22) with the comment that consistency needs rungs to be consistent with; `.btn` got only `.btn-compact` (22px), so every place that wanted 24, 26, 28, 32, 34 or 36 wrote a descendant override. `cp-tx-iconbtn` (24px, used in eight files as `btn btn-ghost cp-tx-iconbtn` on labelled buttons like Move, Cancel, New folder) is then itself re-overridden to 26, 28 and 34 by three more contexts. A maintainer adding a small button has no rung to reach for and will mint a ninth height. `.cp-welcome-cta` shows the other failure mode: its `padding: 10px 26px` is dead because `.btn`'s fixed `height: 30px` still applies, so the welcome CTA is the same 30px as every other button with 14px text overflowing an 8px content box.
  Evidence: Heights on `.btn`-composed elements: 30 src/styles/buttons.css:14; 22 buttons.css:201-209; 24 src/styles/transcript.css:52 `.cp-tx-iconbtn`; 26 transcript.css:632 `.cp-tx-tc-row .cp-tx-iconbtn`, transcript.css:796 `.cp-tx-rename-actions .btn`, transcript.css:870 `.cp-folder-row .btn`, src/styles/ai.css:255 `.cp-ai-export-btn`; 28 transcript.css:1622 `.cp-colorpick-reset`, src/styles/shell.css:922 `.cp-rowmenu-actions .cp-tx-iconbtn`; 32 src/styles/coreview.css:152 `.cp-colobby-cta`; 34 shell.css:936 `.cp-rowmenu-newfolder .cp-tx-iconbtn`; 36 buttons.css:594 `.cp-sbtn-export`. Composition confirmed in TSX, e.g. TranscriptViewer.tsx:1798 `btn btn-ghost cp-tx-iconbtn`, LibraryBrowserBar.tsx:176-177, ProjectMenu.tsx:133-134. Select ladder: buttons.css:672-726. Welcome: src/styles/welcome.css:123-129 on `btn cp-welcome-cta` (WelcomeScreen.tsx:76).
  Fix: In buttons.css add `.btn.btn-sm { height: 26px; padding: 0 10px; font-size: var(--text-base); border-radius: var(--r-sm); }` and `.btn.btn-lg { height: 36px; padding: 0 18px; font-size: var(--text-lg); }` (mirroring `.cp-select.sm/.xs`), keep `.btn-compact` as the 22 rung. Replace the eleven context overrides with the rung class in JSX, rename `cp-tx-iconbtn` to `btn-sm` (it is not an icon button), and give `.cp-welcome-cta` `height: auto` or `btn-lg`.
- **[medium] Disabled treatment has eight opacities and two cursors, with one class overriding `.btn`'s own disabled value on the same element**
  There is no token or rule for what disabled looks like, so each family guessed: opacity .3/.32/.35/.4/.45/.5/.55/.7 and cursor `not-allowed` in twelve families versus `default` in eleven. The only reasoned deviation is `.cp-sbtn[data-phase=loading] { cursor: progress }`. `.cp-colobby-cta:disabled { opacity: .45 }` sits on `btn cp-colobby-cta` and overrides `.btn:disabled`'s .35, so the co-review CTAs are the only `.btn`s with a different disabled look, for no stated reason. `.cp-gen-btn:disabled` only changes the cursor and dims a pseudo-element wash from .6 to .25; label, border and bevel stay at full strength.
  Evidence: opacity .3: src/styles/library.css:1649, src/styles/sidebar.css:541; .32: src/styles/transcript.css:51; .35: src/styles/buttons.css:46, src/styles/room.css:444, src/styles/monitor.css:834, src/styles/transport.css:133, sidebar.css:280, src/styles/toolbar.css:87; .4: src/styles/review.css:86, monitor.css:1267, room.css:775, transcript.css:331, library.css:2185; .45: library.css:2562, src/styles/coreview.css:173; .5: review.css:708, src/styles/ai.css:72, buttons.css:710; .55: monitor.css:371, transcript.css:942; .7: src/styles/queue-drawer.css:184. cursor default: transcript.css:51, toolbar.css:87, room.css:775, transcript.css:331, review.css:708, ai.css:72, monitor.css:371, library.css:973, library.css:1649, library.css:663, buttons.css:249. Override: coreview.css:173 on `btn cp-colobby-cta` (CoReviewLobby.tsx:252). Generate: buttons.css:249 and :289.
  Fix: Add `--opacity-disabled: 0.35;` to src/styles/tokens.css and a base rule `button:disabled { opacity: var(--opacity-disabled); cursor: not-allowed; }` in base.css; delete the per-family opacity/cursor lines (keep `.cp-sbtn` progress and any that dim differently on purpose, with the reason written at the site). Give `.cp-gen-btn:disabled` the same opacity. Write the cursor policy (not-allowed) into docs/DESIGN.md under Targets.
- **[medium] `.btn-icon` is resized or flattened by six contexts, three of which strip the border but keep the bevel, and the primitive has no `:disabled`; its near-namesake `.cp-icon-btn` is a different button**
  `.btn-icon` is a 30px bevelled chip. Inside the URL pill it becomes 24px flat (documented), in the library view toggle 26x24, in the sort-direction button 28px, and `.cp-recent-reveal`, `.cp-recent-chev` and `.cp-people-collapse` shrink it to 22px and set `border: none` while inheriting the raised gradient, bevel shadow, hover lift and pressed shadow, which is a 22px borderless chip with a 3D edge. `.btn-icon` itself defines no `:disabled`, so the one disabled instance (`cp-recents-trigger`) patched it locally and the next one will not. Meanwhile `.cp-icon-btn` (transport.css) is a 30x28 flat r-sm button used by Transport, SpeedControl and VolumeControl: two classes that are word-permutations of each other with opposite treatments.
  Evidence: src/styles/buttons.css:140-172 (`.btn-icon`, no `:disabled`); src/styles/toolbar.css:63-77; src/styles/library.css:1173-1174, :1165; src/styles/sidebar.css:663 `.cp-recent-reveal { width: 22px; height: 22px; border: none; }`, :668 `.cp-recent-chev`; src/styles/room.css:256 `.cp-people-collapse`; composition Sidebar.tsx:246 `btn-icon cp-recent-reveal`, Sidebar.tsx:928, PeoplePanel.tsx:118. Local disabled patch toolbar.css:87. `.cp-icon-btn` src/styles/transport.css:94-113, monitor.css:814-834.
  Fix: In buttons.css add `.btn-icon.sm { width: 24px; height: 24px; border-radius: var(--r-sm); }` and `.btn-icon.flat { border: none; background: transparent; box-shadow: none; }` (`:hover`/`:active` reset included), plus `.btn-icon:disabled { opacity: var(--opacity-disabled); cursor: not-allowed; transform: none; }`; put `sm flat` on the recent/people buttons and drop their private width/height/border lines. Rename `.cp-icon-btn` to `.cp-transport-iconbtn` (three components) so the two names stop colliding.
- **[medium] The review panel has one icon-button recipe at three sizes with two different disabled treatments and inconsistent aria-pressed**
  `.cp-review-iconbtn` (26px, r-sm) and `.cp-review-tool` (34px, r-md; 28px inside reply/edit) share fill, border, hover and `.active` byte for byte, then diverge on disabled (.4 not-allowed vs .5 default). The header toggles carry `aria-pressed`; the composer's draw/label/range toggles, which use the same `.active` class, do not.
  Evidence: src/styles/review.css:67-86 vs :689-708 vs :500. ReviewPanel.tsx:1377, :1394, :1431 have `aria-pressed`; ReviewPanel.tsx:1741, :1751, :1778 toggle `active` with no `aria-pressed`.
  Fix: Collapse to `.cp-review-iconbtn` with `.lg` (34px) and `.md` (28px) size modifiers, one `:disabled`; add `aria-pressed={drawActive && !labelActive}` etc. to the three composer toggles.
- **[medium] The ON/pressed state is drawn at three different brightnesses, and `.btn-icon.active` means two different things in two toolbars**
  base.css says emphasis is brightness, not hue, but the brightness has no token. Pressed icon buttons are bg-5 (#403F46) in the transport family, bg-3 (#1E1D20) in nav/reader/library, and 5 to 16 percent white in the toolbar, transcript and settings families. The sidebar and queue toggles (`.btn-icon.active`, 6% white) sit in the same window as the library view toggle (`.btn-icon.active`, solid bg-3). `--ring-selected`, whose comment says it is for toggles ON, is referenced by one rule.
  Evidence: bg-5: src/styles/transport.css:113, :77; src/styles/room.css:439; src/styles/transcript.css:282; src/styles/monitor.css:598, :832. bg-3: src/styles/nav.css:67; monitor.css:1404; src/styles/library.css:1174, :1007; src/styles/shell.css:1086, :996; transcript.css:941. white alpha: src/styles/settings.css:146 (5%); src/styles/queue-drawer.css:447 and src/styles/toolbar.css:200 (6%); monitor.css:554 and room.css:770 (8%); transcript.css:60 (accent 8%); transcript.css:1228 (9%); src/styles/review.css:81 and :703 (accent 16%); queue-drawer.css:177 (9->3% gradient). `--ring-selected` defined src/styles/tokens.css:100-108, used only settings.css:288.
  Fix: Add `--fill-on: var(--bg-3);` (or bg-5, one decision) and `--ink-on: var(--fg-1);` to tokens.css and use them in every `.active` / `.on` / `[aria-pressed=true]` rule above; either apply `--ring-selected` to those states or delete the token and its comment.
- **[medium] Danger text uses `--danger` on three buttons where the palette says `--danger-text`, and one of them is below AA in a state the contrast sweeps never reach**
  tokens.css names `--danger-text` (#FF8A8A) precisely because `--danger` (#E82626) fails on dark greys as text. `.cp-room-end` paints its 12px bold label in `--danger` on the titlebar (#18181B per the tokens comment), which is roughly 4.0:1, under the 4.5:1 floor. Two hover states do the same. Nine other danger buttons use `--danger-text`. Neither contrast sweep enters a live session, so the room bar is unmeasured.
  Evidence: src/styles/room.css:456-470 `.cp-room-end { color: var(--danger); ... }` and `:hover { color: var(--danger) }`; src/styles/ai.css:433 `.cp-ai-chapter-del:hover { color: var(--danger) }`; src/styles/library.css:2384 `.cp-selbar-danger:hover { color: var(--danger) }`. The convention: src/styles/coreview.css:737, queue-drawer.css:417, review.css:286, transcript.css:1480, room.css:438, :1025, library.css:2224. Token rationale src/styles/tokens.css:68-73. Sweep scope e2e/contrast.spec.ts:130-134, e2e/deep-state-contrast.spec.ts:148-178 (no session state).
  Fix: Change the three to `var(--danger-text)`; add a hosted-session state to deep-state-contrast.spec.ts so the room bar is measured.
- **[medium] Icon buttons that float over imagery use five different scrim recipes, and one hover-revealed action uses `display: none` where every other one uses opacity**
  The thumbnail card actions, web-card collect button, library card menu, room person controls and frame-viewer step buttons are all the same job (a control readable over a picture) and each mixes its own backdrop: black 65% with blur, black 55%, bg-0 72% with a line-3 border, bg-0 72% with line-2, bg-2 82% as a pill. `.cp-grants-revoke` and `.cp-person-ctl` are otherwise twins (24px grid-centred, line-2, r-sm, 2px outline ring). Separately, `.cp-recents-remove` reveals with `display: none -> inline-flex` while the other nine hover-revealed actions use `opacity: 0` plus `:focus-visible { opacity: 1 }`, so it is the one that cannot fade and the one whose reveal rule cannot be reasoned about the same way.
  Evidence: src/styles/sidebar.css:260 `.cp-thumb-actions button` (rgba(0,0,0,0.65) + blur); src/styles/library.css:2096 `.cp-web-collect-btn` (rgba(0,0,0,0.55)); library.css:601 `.cp-lib-more` (bg-0 72%, line-3); src/styles/room.css:992 `.cp-person-ctl` (bg-0 72%, line-2); library.css:2304 `.cp-framepv-step` (bg-2 82%, pill); src/styles/coreview.css:726 `.cp-grants-revoke`. Reveal outlier src/styles/toolbar.css:160-179 versus library.css:371, :622, :2009, :2080, :2108; transcript.css:1278, :1449; review.css:1276; room.css:971.
  Fix: One `.cp-overlay-iconbtn { width: 26px; height: 26px; border: 1px solid var(--line-2); border-radius: var(--r-sm); background: color-mix(in srgb, var(--bg-0) 72%, transparent); backdrop-filter: blur(8px); }` used by all six; switch `.cp-recents-remove` to the opacity pattern with `:focus-visible { opacity: 1 }`.
- **[medium] Flat borderless icon buttons exist at 24, 26 and 30x28 with five hover fills, with no stated reason for the sizes**
  Same intent (a transparent icon button inside a panel), same resting look, but the queue uses 24px, the reader stage, library tree, annotation palette and speaker roster use 26px, the transport uses 30x28, and hover is 5, 6, 7, 8 or 9 percent white or solid bg-3. None of these sizes is explained in a comment; the only explained size variant in the app is `.cp-icon-btn.speed` (carries text).
  Evidence: src/styles/queue-drawer.css:400 (24, hover 5%); src/styles/shell.css:542 (26, hover bg-3); src/styles/library.css:957 (26, 6%); src/styles/monitor.css:1202 (26, 8%); src/styles/transcript.css:1178 (26, 7%); src/styles/library.css:1426 and queue-drawer.css:255 (24, 8% / 6%); src/styles/transport.css:94 (30x28, 5%).
  Fix: Fold these into the `.btn-icon.flat` / `.btn-icon.sm` modifiers from the `.btn-icon` finding (26px default, 24 for `.sm`), with one hover fill (`rgba(255,255,255,0.08)`), and delete the private classes.
- **[medium] DESIGN.md says button labels take `--track-wide`; the base `.btn` uses `--track-tight`, and other button families use three other rungs**
  The doc and the primary class contradict each other, so a new button will copy whichever the author read last. `.btn` letter-spaces 12px labels at -0.01em, the rung the token comment reserves for large display text; segmented buttons and the view trigger use wide, the Generate button slight, compact buttons caps.
  Evidence: docs/DESIGN.md:50-52 (`--track-wide` for small body and buttons). src/styles/buttons.css:32 `.btn { letter-spacing: var(--track-tight) }`; src/styles/tokens.css:247-249 (tight = large display text, wide = button labels). Others: sidebar.css:518 wide, monitor.css:532 wide, buttons.css:232 slight, buttons.css:205 caps.
  Fix: Either change buttons.css:32 to `var(--track-wide)` (and `.cp-gen-btn` to match) or rewrite DESIGN.md:52 to say buttons use tight; the design-tokens contract only checks that a `--track-*` token is used, so the doc has to carry the role.
- **[medium] hit-target-contract cannot see a button whose className is `"btn-icon cp-x" + (cond ? ...)`, and two 22px buttons hide behind exactly that shape**
  The ratchet's scraper matches a string-literal className or an expression whose first quoted string starts with `cp-`. A className that starts with `btn-icon ` and then a `cp-` class is skipped, as is any expression beginning with a template or variable. A brace-aware scan finds 219 button classes; the contract's regex finds 174. Two of the 45 missing are sub-24 squares that are neither in KNOWN_SMALL nor justified anywhere.
  Evidence: src/lib/hit-target-contract.test.ts:63 `/<button\b[^>]*?className=(?:"([^"]+)"|\{[^}]*?"(cp-[\w-]+)")/gs`. Sidebar.tsx:928 `className={"btn-icon cp-recent-chev" + (... ? " open" : "")}` with src/styles/sidebar.css:668 `.cp-recent-chev { width: 22px; height: 22px }`; PeoplePanel.tsx:118 `className={"btn-icon cp-people-collapse" + (collapsed ? " open" : "")}` with src/styles/room.css:256 `.cp-people-collapse { width: 22px; height: 22px }`. The test's own comment (lines 26-29) admits the gap but describes it as runtime-assembled classes only.
  Fix: Replace `buttonClasses()` with the brace-aware `buttonTags()` extractor already written in src/lib/mousedown-activation-contract.test.ts (lines 40-58) and collect every `cp-` token from the whole className expression; then either grow `.cp-recent-chev` / `.cp-people-collapse` to 24 or add them to KNOWN_SMALL with the spacing argument in the commit.
- **[low] The same control height gets a different radius depending on whether it is a select or a button, and `.cp-gen-btn` borrows the floating-surface radius**
  `.cp-select` ties radius to height (30 r-md, 26 r-sm, 22 r-xs). Buttons at 22px use r-sm, r-xs and r-lg; at 26px r-md and r-sm. A 26px `.cp-select.sm` next to a 26px `.cp-folder-row .btn` has a 6px corner beside an 8px one. `.cp-gen-btn` uses `--r-card`, which DESIGN.md reserves for surfaces that float.
  Evidence: 22px: src/styles/buttons.css:208 (r-sm), src/styles/transcript.css:923 (r-xs), src/styles/logs.css:72 (r-xs), src/styles/review.css:1124 (r-lg, a pill in effect), buttons.css:720 (r-xs). 26px: transcript.css:870 (r-md inherited), buttons.css:716 (r-sm), review.css:67 (r-sm), monitor.css:532 (r-sm). buttons.css:236 `.cp-gen-btn { border-radius: var(--r-card) }` versus docs/DESIGN.md:142.
  Fix: Have the `.btn-sm` / `.btn-compact` rungs from the size finding carry r-sm / r-xs so the two ladders agree; set `.cp-review-verpill` to `var(--r-pill)`; set `.cp-gen-btn` to `var(--r-md)` or write at the site why a control takes the card radius.
- **[low] Three token bypasses on buttons that the existing contracts cannot see: a `font:` shorthand, non-hex fallbacks, and an rgba form of a folded colour**
  `.cp-tx-follow-pill` sets `font: 600 11px var(--font-sans)` so a raw size and weight ride past design-tokens-contract, which matches `font-size:` and weight names only. Four button rules carry `var(--r-md, 8px)` and one `var(--dur-fast, 140ms)` (the token is 120ms), the exact stale-fallback failure token-fallback-contract exists for, but it matches hex fallbacks only. `.cp-review-tool.recording` writes `rgba(255, 107, 107, ...)`, which is #ff6b6b, the colour the tokens file says was folded into `--danger-text`; token-usage-contract matches `#hex` only.
  Evidence: src/styles/transcript.css:1044-1063 (`font: 600 11px ...`, `var(--dur-fast, 140ms)`, `var(--ease-out, ease)`); src/lib/design-tokens-contract.test.ts:425. src/styles/coreview.css:152, room.css:456, :600, :781 `var(--r-md, 8px)`; src/lib/token-fallback-contract.test.ts:43 (`#[0-9A-Fa-f]{3,8}` only). src/styles/review.css:709-716 and :827; tokens.css:69-73; src/lib/token-usage-contract.test.ts:70.
  Fix: Edit the six sites (`font-size: var(--text-base); font-weight: var(--weight-semibold)`, drop the fallbacks, `color-mix(in srgb, var(--danger-text) 16%, transparent)`), then widen the three regexes: match `font\s*:` shorthands, any literal fallback on a defined token, and `rgba?\(` triples that equal a token's hex.
- **[low] Reduced-motion treatment of hover lifts and scales is decided per button rather than once**
  Under `prefers-reduced-motion`, `.btn:hover` keeps its 1px lift (only the duration is zeroed), `.cp-gen-btn:hover` and `.cp-react-pop-btn:hover` have their transform removed, and `.cp-react-pick-btn:hover` (the room's twin of the review reaction button) keeps its scale(1.12). reduced-motion-contract guards keyframes, not these.
  Evidence: src/styles/buttons.css:657-661 (duration only) vs :507 (`transform: none`); src/styles/review.css:996 `.cp-react-pop-btn:hover { transform: none }` vs src/styles/room.css:599 and :1039 (duration only for `.cp-react-pick-btn`).
  Fix: Pick one policy (snap, or remove) and apply it to all four in the same reduced-motion block; if snap, delete buttons.css:507 and review.css:996; if remove, add `.btn:hover, .btn-icon:hover, .cp-react-pick-btn:hover { transform: none }` to buttons.css:657.
